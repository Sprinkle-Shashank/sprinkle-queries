with table1 as
(select month_timerange, conductedby, events, cond.name as conductedby_update ,
case
when events = 'true' then 1 else 0
end as Event_count
from profile_60433 as m
 
 left join ds_mysql_prod_tagelement cond
 on p.conductedby = cond.id
),
 
 table2 as
 ( 
   select * from table1 as p
   )
 
 select * from table2 as p
 
 /*
 
 ,
   
   table3 as
   ( 
	 select  month_timerange, conductedby, events, event_count , d1_new.name as conductedby_update_new from table2 as p
	 
	 left join ds_mysql_prod_tagelement d1_new
	 on p.d1 = d1_new.id
 )
 
 */
 