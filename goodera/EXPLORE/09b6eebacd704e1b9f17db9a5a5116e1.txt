SELECT projectid, project_code_new, amount,
t.name AS fy_timerange

FROM profile_55194 AS PF

LEFT JOIN ds_mysql_prod_tagelement AS t
ON PF.fy_year = t.id

--WHERE project_code_new IS NULL

--GROUP BY projectid, fy_timerange, project_code_new

--ORDER BY project_code_new ASC