Select pro.name as projectId,pro2.name as parentproject,fy_timerange,SUM(prescribed_csr) as prescribed_csr from profile_78820

left join ds_mysql_prod_project as pro
  on p.projectid = pro.id
 left join ds_mysql_prod_project as pro2
  on pro.parent = pro2.id
  