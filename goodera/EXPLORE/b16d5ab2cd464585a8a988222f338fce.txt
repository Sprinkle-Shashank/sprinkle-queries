Select distinct(fellow),school,t.name as grade,CAST(fy_timerange.start AS DATE) AS start,
EXTRACT(YEAR FROM start) AS year,
'FY '||year || ' - '||year+1 AS financial_year
From profile_55396
left join ds_mysql_prod_tagelement t
on t.id=grade