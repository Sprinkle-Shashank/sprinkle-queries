with x as (
SELECT projectid, _id,
 CAST(data.name as VARCHAR),
 CAST(data.sum as INT)
from ds_mongo_profileinstance
where profileId='79211' and active = 't' and securitycontextid is not null )
select projectid, sum(sum)
from x
group by projectid 


 