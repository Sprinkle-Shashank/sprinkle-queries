with table1 as
(
  select p.projectid , p._id , p.projects , fy_timerange , sta.name as state_update , scho_type.name as school_type , schools_reach , students_reach , cost_per_child , books_distributed , bags_distributed , ready_reckoners , shoes , crayons , geometry_box , learning_aid , pwd_kits , case when photo is null then '-' else 'Download' end as photo_update , case when upload is null then '-' else upload end as upload_update from profile_51203 as p
  
  left join ds_mysql_prod_tagelement sta
  on p.state = sta.id
  
  left join ds_mysql_prod_tagelement scho_type
  on p.type_of_school = scho_type.id
  )
  
  select * from table1