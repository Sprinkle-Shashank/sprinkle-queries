with t1 as 
(select projectId, name, fy_timerange, head, amount, entity, fy_timerange.start as start_date 
from profile_82741),

t2 as 
(select projectId, name, fy_timerange, head, amount, entity, start_date :: date 
from t1)

select projectId, name, fy_timerange, head, amount :: int, entity, to_char(start_date, 'DD-MM-YYYY') as start_date, to_char(start_date, 'Month') as month, to_char(start_date, 'YYYY') as year
from t2
