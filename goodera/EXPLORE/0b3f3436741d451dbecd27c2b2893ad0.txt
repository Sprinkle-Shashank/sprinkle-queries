WITH sub1 AS(SELECT pro.name AS projectId, p.fy_timerange, p.title, p.photo, p.date, p.location, p.people from profile_80488 p
			
LEFT JOIN ds_mysql_prod_project AS pro
ON p.projectid = pro.id
)

SELECT s1.*,CAST(c AS varchar)  FROM sub1 s1, s1.photo C
  ORDER BY s1.fy_timerange DESC
