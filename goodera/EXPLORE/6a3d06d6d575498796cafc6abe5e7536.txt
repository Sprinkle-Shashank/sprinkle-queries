SELECT * 
FROM
(Select id, projectId, fy_timerange, state, phc, topic, hw_type, pic_1, cap_1, count_trainings, attendees from profile_76552 p



UNPIVOT (
    val FOR dim IN (attendees, count_trainings)) 


/*
Group By  projectId, fy_timerange, state, phc, topic, hw_type, State, name, dim

SELECT * 
FROM (SELECT pro.name AS projectid, fy_timerange, helper, toilet_pans FROM profile_74669 p 
	  LEFT JOIN ds_mysql_prod_project pro
on p.projectid = pro.id) 
UNPIVOT (
    val FOR dim IN (helper, toilet_pans)
)

