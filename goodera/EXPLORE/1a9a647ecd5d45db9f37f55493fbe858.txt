select project.name project_name, p.fy_timerange, p.photo from
(select m.*, photo.photo from profile_79597 m, m.photo photo) p 
left join ds_mysql_prod_project as project
on p.projectid = project.id
group by 1,2,3;