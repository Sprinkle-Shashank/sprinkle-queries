select fy, upper(code.state) as No_of_states,focus_area from (select SUBSTRING(PROPOS,charindex('|',PROPOS)+1,6) as censuscode,fy,focus_area from
(select SUBSTRING(ProjName,charindex('|',ProjName)+1,100) as PROPOS, fy,focus_area from (select  prod.name as ProjName, p.fy, p.focus_area from
profile_59426 p 
left join 
ds_mysql_prod_project prod on
prod.id=p.projectid)))
left join Village_census_code_profile_27489 code on
code.name=censuscode 
 




