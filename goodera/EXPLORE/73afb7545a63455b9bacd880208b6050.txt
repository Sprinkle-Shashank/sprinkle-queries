SELECT sq.country AS Region, sq.fy_timerange, sq.cdms_id AS pvh_id, sum(hours) as hours, 
case
	when sq.program='' then 'HERproject'
	else Null
end) AS program
FROM profile_62972 sq
GROUP BY sq.country, sq.fy_timerange, sq.cdms_id