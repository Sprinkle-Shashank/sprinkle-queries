with 
(
  select tag4.name as projectId ,p1.details_new,tag3.name as status , tag1.name as training_type,tag2.name as year
  --tag4.name as unwind_details_new 
  from profile_64806 as p1

left join profile_61928 as p2
on p1.details_new = p2._id
left join ds_mysql_prod_tagelement tag1
on p1.training_type = tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.year = tag2.id
left join ds_mysql_prod_tagelement tag3
on p1.status = tag3.id
left join ds_mysql_prod_project tag4
on p1.projectId = tag4.id
-
),


with sub1 as (select a.*, unwind_details_new from t1 as a, a.details_new  as unwind_details_new)
select  c.projectId, tag5.name as details_new,c.status,c.training_type,c.year
left join ds_mysql_prod_tagelement tag5
on c.unwind_details_new = tag5.id
from sub1 c