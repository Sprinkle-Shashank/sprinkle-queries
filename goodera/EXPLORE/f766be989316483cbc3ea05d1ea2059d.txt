
with sub1 as (select a.*, unwind_details_new from profile_64806 as a, a.product_category as unwind_details_new),
select p1.projectId ,p1.details_new ,tag3.name as status , tag1.name as training_type,tag2.name as year from sub1 p1

--left join profile_61928 as p2
--on p1.details_new = p2._id
left join ds_mysql_prod_tagelement tag1
on p1.training_type = tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.year = tag2.id
left join ds_mysql_prod_tagelement tag3
on p1.status = tag3.id