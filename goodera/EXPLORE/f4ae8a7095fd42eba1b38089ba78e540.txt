with phase1 as (
	select proj.name,p2.name,(cast(p.fy_timerange.start as varchar)||'|'||cast(p.fy_timerange.end as varchar)) as fy_timerange from profile_58861 p
  	left join ds_mysql_prod_project proj
  	on p.projectid = proj.id
 	left join profile_58845 p2
	on p.location = p2._id
)
select * from phase1