with a as (
  	select json_array_length(p.members_saved) as memsave, json_array_length(p.members_double_saved) as memdouble, p.participants, p.amount_saved, proj.name as projectid,  p.fy_timerange
    from profile_58862 p
  	left join ds_mysql_prod_project proj
  	on p.projectid = proj.id 
  	
)/*, b as (
	select (a1.participants - a1.memsave) as tpm from a a1
), c as (
	select (b1.tpm + a1.memdouble) as tp from a a1,b b1
), d as (
	select (a1.amount_saved * c1.tp) as ts from a a1,c c1
)
select a1.projectid,a1.fy_timerange,SUM(d1.ts) as total_savings from a a1,d d1
GROUP BY 1,2
*/
select * from a