with table1 as(select pro.name as projectid ,p.fy_timerange,amount,q.states from profile_77073 p 
left join ds_mysql_prod_project pro 
on p.projectId = pro.id

left join profile_46742 q
on p.projectid = q.projectid),

table2 as(select projectid,fy_timerange,amount , state from table1 t1 , t1.states state)

select * from table2
--group by pro.name,fy_timerange

