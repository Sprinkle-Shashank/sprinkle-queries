SELECT approvalLog[0].["approvalStatus"] as a1,
count(
	  CASE
	  	WHEN approvalLog[0].["approvalStatus"] like '%APPROVED%' then 'a2'
  		WHEN approvalLog[0].["approvalStatus"] like '%REJECTED%' then 'a2'
 		WHEN approvalLog[0].["approvalStatus"] like '%PENDING_APPROVAL%' then 'a2'
		end) as a3
		

FROM taskresp_27398
group by 1
