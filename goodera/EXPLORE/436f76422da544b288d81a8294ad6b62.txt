with sub1 as(
  select p1._id as id, p2.name as projectId, p1.name, p1.title, p1.desc, p1.photo, p1.timerange_timerange as fy_timerange, p1.url
	from profile_62850 p1
	left join ds_mysql_prod_project p2 on p2.id = p1.projectid)

select s1.*, photos from sub1 s1, s1.photo photos
where get_array_length(s1.photo) > 1
