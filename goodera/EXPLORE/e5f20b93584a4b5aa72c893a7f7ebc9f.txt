with table1 as
(select _id , projectId,country, vendor,unwind_cycle,factory, product_category, consultant_tag from profile_64362 as p ,p.cycle as unwind_cycle),

 table2 as
(select _id, projectId,country, vendor,cycle,factory,product_category,consultant_tag from profile_64362
  where _id not in (select _id from table1))
  ,
  
table3 as
 (
	select * from table1
  Union all
  select * from table2
 )
 ,

  
table4 as  
(
 select _id,  projectId,country,vendor,unwind_cycle,factory, unwind_product_category,consultant_tag from table3 t3, t3.product_category  as unwind_product_category
 ),

table5 as
(select _id, projectId,country,vendor,factory,unwind_cycle,  null as product_category,consultant_tag from table3 
 where _id not in (select _id from table4)),
 
table6 as
(select * from table4
union all
select * from table5),

t1 as (select tag2.name as projectId, p4.factory,p1.vendor,p1.factory_id,tag5.name as consultant,tag6.name as training_type,tag1.name as year,tag7.name as country,tag9.name as product_category,tag8.name as cycle,
cast('1' as VARCHAR) as total_fields
from profile_73974 p1
	   
--left join profile_64362 p5
--on p1.projectId = p5.projectId
	   
--left join profile_64170 as p3
--on p5.factory = p3._id
	   
left join table6 p4
on p1.projectId = p4.projectId
left join ds_mysql_prod_tagelement tag9
on p4.unwind_product_category= tag9.id
left join ds_mysql_prod_tagelement tag1
on p1.year= tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.projectId = tag2.id
left join ds_mysql_prod_tagelement tag3
on p4.factory = tag3.id
left join ds_mysql_prod_tagelement tag5
on p1.consultant_tag = tag5.id
left join ds_mysql_prod_tagelement tag6
on p1.training_type = tag6.id
left join ds_mysql_prod_tagelement tag7
on p1.country = tag7.id
left join ds_mysql_prod_tagelement tag8
on p1.cycle = tag8.id
where p1.projectId != 47042 or p1.projectId != 47043 or p1.projectId != 47847 or p1.projectId != 47849 or p1.projectId != 47851 or p1.projectId != 47853 or p1.projectId != 47855 or p1.projectId != 47857 or p1.projectId != 47049 or p1.projectId != 47846 or p1.projectId != 47854 or p1.projectId != 47856
--group by p1.projectId, p1.factory,p1.vendor,p1.factory_id,p1.consultant,training_type,year,p1.country,p1.product_category,p1.cycle,tag1.name,tag2.name,tag3.name,tag5.name,tag6.name,tag7.name,tag8.name,p3.factory,tag9.name
	  )

select * from t1
group by projectId, vendor, factory_id, consultant, training_type, year, country, product_category, cycle, factory,t1.total_fields