with table1 as
( select *, proj.name as projectname from profile_82951 as p

left join ds_mysql_prod_project proj
on p.projectid = proj.id),

table2 as
(
  select * , p1 from table1 as p, p.file as p1)
  

select *, cast(p1 as varchar) as file_update from table2 as p
