
with table1 as 
(
  select p.name  as name, num_females , max(num_females) as max_update ,row_number() over 
(partition by p.projectid
 order by p._id desc) as row from profile_4227 as p

group by p.name , num_females , p.projectid , p._id 
  )
  
  select name , max_update from table1 where row=1;