with table1 as
( select p.projectid , p._id , name_subunit , case when name_subunit is null or name_subunit=' ' then 'N/A' else name_subunit end as unit , status_update.name as status_1 from profile_16619  as p
  
left join ds_mysql_prod_tagelement name_subunit_update 
on p.name_subunit = name_subunit_update.id  
 
left join ds_mysql_prod_tagelement status_update 
on p.status = status_update.id
),

table2 as 
( 
 select p.projectid , p._id , sum(day_1_workers) as day_1_workers_update , sum(last_day_workers) as last_day_workers1_update , sum(p1.total_workers) as total_workers_update 

, fac.name as factory , cou.name as country ,ven.name as vendor , fy1.name as fy , t1.unit
  
 from profile_16862 p
  
  left join profile_17526 p1
  on p.projectid =p1.projectid
  
left join ds_mysql_prod_project fac
on p.projectid = fac.id
  
left join ds_mysql_prod_project ven
on fac.parent = ven.id
  
left join ds_mysql_prod_project cou
on ven.parent = cou.id
  
left join ds_mysql_prod_tagelement fy1 on
p.fy = fy1.id
  
left join table1 t1 on 
p.projectid = t1.projectid
  
where p.projectId ilike '%%' and cou.name <> 'Test Country' and country <> 'GAP Inc.' and (t1.status_1='Completed' or t1.status_1 is null) 
  
 group by p.projectid , day_1_workers , last_day_workers , p1.total_workers , ven.name , fac.name , cou.name , fy1.name , t1.unit , p._id
)

 select * from table2

/*  
, ven.name ,cou.name ,fac.name ,fy1.name, p.sub_unit
  --left join profile_16619 p2
--on p.projectId = p2.projectid
  
 left join ds_mysql_prod_project fac
on p.projectid = fac.id
  
left join ds_mysql_prod_project ven
on fac.parent = ven.id
  
left join ds_mysql_prod_project cou
on ven.parent = cou.id
  

  left join ds_mysql_prod_tagelement fy1 on
  p.fy = fy1.id
 
cou.name as country , 
nvl2(SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)),ven.name) as vendor_name, case when vendor_name='' then ven.name else vendor_name end as vendor, fac.name as factory ,
fy1.name as fy,-- quarter1.name as quarter_fy,
case when p.sub_unit is null or p.sub_unit=' ' then  'N/A' else p.sub_unit end  as unit  


  --left join ds_mysql_prod_tagelement quarter1 on
  --p.quarter = quarter1.id
--, p2.status

  --and (p2.status = 1400365 or p2.status is null)
  
  and cou.name <> 'Test Country'  and p1.projectId is not null --and country <> 'GAP Inc.' 

select p.projectId, p1.status, cou.name as country ,p2.total_workers, 
nvl2(SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)),ven.name) as vendor_name, case when vendor_name='' then ven.name else vendor_name end as vendor, fac.name as factory ,
case when p.sub_unit is null or p.sub_unit=' ' then  'N/A' else p.sub_unit end  as unit,
--cast(DATEPART(YEAR,p1.date_enrollment) as varchar) as year_new ,
fy1.name as fy,
p.day_1_workers , p.last_day_workers , p.workers_exited , p.absenteeism_rate , p.labor_disputes , p.bc_meetings ,
p.person_days_lost ,p.production_efficiency , 1 as count, quarter1.name as quarter_fy,  
((last_day_workers - day_1_workers)/nullif(p2.total_workers,0) )*100 as workersexited   
from profile_16862 p
  
  
left join ds_mysql_prod_project fac
on p.projectid = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id
left join ds_mysql_prod_project cou
on ven.parent = cou.id
left join profile_16619 p1
on p.projectId = p1.projectid
left join profile_17526 p2
on p.projectId =p2.projectid
  left join ds_mysql_prod_tagelement quarter1 on
  p.quarter = quarter1.id
  left join ds_mysql_prod_tagelement fy1 on
  p.fy = fy1.id
  
where  p.projectId ilike '%%' and cou.name <> 'Test Country'  and p1.projectId is not null and country <> 'GAP Inc.' --and (p1.status = 1400365 or p1.status is null)

group by  p.projectId, p1.status, cou.name , ven.name, fac.name , p.sub_unit ,-- p1.date_enrollment ,
p.day_1_workers , p.last_day_workers , p.workers_exited , p.absenteeism_rate , p.labor_disputes , p.bc_meetings , p.person_days_lost , quarter1.name , fy1.name ,p.production_efficiency ,p2.total_workers */