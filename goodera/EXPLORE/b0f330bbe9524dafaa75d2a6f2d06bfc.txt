select p._id as id, p1.name as site, (p.date||'|'||p.date) as fy_timerange, sum(p.vials_returned) as vials_returned
from profile_78081 p
	left join profile_78079 p1 on p1._id = p.site
	group by id, p1.name, fy_timerange