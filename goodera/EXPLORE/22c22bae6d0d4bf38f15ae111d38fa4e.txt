select tag1.name as Project_Id, select_student, fy_timerange, sum(amount_claimed) as amount_claimed
from profile_64076 p1
left join ds_mysql_prod_project tag1
on p1.projectId = tag1.id
left 
group by tag1.name, select_student, fy_timerange