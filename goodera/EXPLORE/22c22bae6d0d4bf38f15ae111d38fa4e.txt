select projectId, select_student, fy_timerange, tag1.name as select_course, sum(amount_claimed) as amount_claimed
from profile_64076 p1
left join ds_mysql_prod_project tag1
on p1.projectId = tag1.id
group by projectId, select_student, fy_timerange, select_course, tag1.name