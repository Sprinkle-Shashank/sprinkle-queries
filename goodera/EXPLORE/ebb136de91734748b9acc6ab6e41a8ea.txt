with t1 as
(select _id, projectId.name as projectId, date_meeting, participants, upload, minutes, minutes_update, upload_1, agenda, fy_timerange
 
 CASE when upload<>'' 
  then 'Document' else '-' end as Doc_Label,
  
  CASE when upload_1<>'' 
  then 'Document' else '-' end as Updated_Doc_Label
 
 from profile_5660 as m
 
 left join ds_mysql_prod_project proj
  on m.projectId = projectId.id
 
 left join profile_56342 AS r
 	on m.projectId = r.projectId
 
 
