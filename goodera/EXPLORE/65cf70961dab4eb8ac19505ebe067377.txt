with a as(
	select proj.name as projectid, p2.name as location, cast(p1.date_lending as varchar), cast(p1.date as varchar),concat(+91,p1.contact) as contact ,p1.bank_account, tag1.name as gradey from profile_58848 p1
  	left join ds_mysql_prod_project proj
	on p1.projectid = proj.id
	left join profile_58845 p2
	on p1.location = p2._id
  	left join ds_mysql_prod_tagelement tag1
  	on p1.gradey = tag1.id
)
select * from a