select fy_timerange, school_name, state, projectId
from profile_65885

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
left join ds_mysql_prod_tagelement as state
 on m.state = state.id
group by fy_timerange, school_name, state, projectId