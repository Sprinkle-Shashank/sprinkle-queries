with temp1 as
(select projectId.name as profileId, fy_timerange, dealer,count(*) from profile_64881 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id

group by projectId.name , dealer, fy_timerange),
temp2 as
( select * from temp1
 where dealer IS NOT NULL)
 select * from temp2