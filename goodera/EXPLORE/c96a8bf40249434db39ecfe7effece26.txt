select project.name project_name, p.fy_timerange, p.photos, p.title, p.comments from 
(select m.*, photos from profile_79598 m, m.photo photos) p 
left join ds_mysql_prod_project as project
on p.projectid = project.id
group by 1,2,3,4,5;