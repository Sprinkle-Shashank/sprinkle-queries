With tab1 as (Select sum(student) as student,school,t1.name as grade,fellow,CAST(fy_timerange.start AS DATE) AS start,
EXTRACT(YEAR FROM start) AS year,
'FY '||year || ' - '||year+1 AS financial_year
From profile_55396
left join ds_mysql_prod_tagelement t1
on t1.id=grade
group by fy_timerange,school,t1.name,fellow)

Select distinct(tab1.*)
From tab1