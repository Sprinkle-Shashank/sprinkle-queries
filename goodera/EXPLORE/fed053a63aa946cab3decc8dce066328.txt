With tab1 as(Select fellow,school,CAST(fy_timerange.start AS DATE) AS start,
EXTRACT(YEAR FROM start) AS year,
'FY '||year || ' - '||year+1 AS financial_year
From profile_55396
group by fy_timerange,school,fellow)

Select distinct(tab1.*)
from tab1