select    p.donor,p.name, p.fy_timerange,  y.name as Project,concat(concat(substring(p.start,1,10),' - '),substring(p.end,1,10)) as Period , sum(p.amount) as commitment ,sum(h.utilized) as utilized, sum(k.amnt) as disbursed from profile_36980 p

left join profile_56061 h on
p._id=h.donor_com

left join profile_36979 k on
p._id=k.donor_com

--LEFT JOIN ds_mysql_prod_project t on p.projectId=t.id
--LEFT JOIN ds_mysql_prod_project t1 on t.parent=t1.id      
--LEFT JOIN ds_mysql_prod_project t2 on t1.parent=t2.id

LEFT JOIN ds_mysql_prod_project y on p.projectid=y.id

Group by y.name, p.donor, p.fy_timerange, Period,p.name