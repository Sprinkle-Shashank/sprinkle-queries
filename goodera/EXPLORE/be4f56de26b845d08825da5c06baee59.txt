with table1 as(
	select t.name as reason,
  	count(t.name) as count
    from profile_58511 p left join ds_mysql_prod_tagelement t
  	on p.reason_camp_n = t.id
  	group by t.name
)
with table2 as(
    select sum(count)
	from table1
)
select reason,count
from table1