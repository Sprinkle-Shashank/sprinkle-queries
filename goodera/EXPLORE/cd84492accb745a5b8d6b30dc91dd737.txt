 with table1 as
(select _id , projectId,country, vendor,unwind_cycle, product_category, consultant_tag from profile_64362 as p ,p.cycle as unwind_cycle),

 table2 as
(select _id, projectId,country, vendor,cycle,product_category,consultant_tag from profile_64362
  where _id not in (select _id from table1))
  ,
  
table3 as
 (
	select * from table1
  Union all
  select * from table2
 )
 ,

  
table4 as  
(
 select _id,  projectId,country,vendor,unwind_cycle, unwind_product_category,consultant_tag from table3 t3, t3.product_category  as unwind_product_category
 ),

table5 as
(select _id, projectId,country,vendor,unwind_cycle,  null as product_category,consultant_tag from table3 
 where _id not in (select _id from table4)),
 
table6 as
(select * from table4
union all
select * from table5),


table7 as
(select projectId,_id, status, training_type,  year, unwind_details_new from profile_64806 as p1,p1.details_new as unwind_details_new),

table8 as
(select _id, projectId,status, training_type,  year, null as details_new from profile_64806
 where _id not in (select _id from table7)),

  
  table9 as
 (
   select * from table7
   Union all
   select * from table8
 )

, t2 as (
  select  tag14.name as unwind_cycle ,tag15.name as unwind_product_category from table6
  left join ds_mysql_prod_tagelement tag15
on unwind_product_category = tag15.id
left join ds_mysql_prod_tagelement tag14
on unwind_cycle = tag14.id
  
),
t3 as (
  select   cast(unwind_details_new as varchar) , p6.name as dn , listagg(dn)
  from table9  as t
  left join profile_61928 as p6
  on  t.projectId = p6.projectId
 -- left join ds_mysql_prod_tagelement tag16
--on unwind_details_new = tag16.id
  group by t.unwind_details_new ,p6.name
)
 
 select * from t3
 
/*select  p2._id, p3.name as projectId, tag7.name as status,tag1.name as  training_type, tag2.name as year,tag3.name as country,p4.vendor, tag4.name as consultant ,tag5.name as cycle,tag6.name as  product_category , cast(p2.unwind_details_new as varchar) , tag8.name as  unwind_details_new   from table9 as p2   

left join table6 as p4
on p2.projectId = p4.projectId
left join table9 as p5
on p2.projectId = p5.projectId
left join ds_mysql_prod_project p3
on p2.projectId= p3.id
left join ds_mysql_prod_tagelement tag1
on p2.training_type = tag1.id
left join ds_mysql_prod_tagelement tag2
on p2.year = tag2.id
left join ds_mysql_prod_tagelement tag7
on p2.status = tag7.id
left join ds_mysql_prod_tagelement tag3
on p4.country = tag3.id
left join ds_mysql_prod_tagelement tag4
on p4.consultant_tag = tag4.id
left join ds_mysql_prod_tagelement tag5
on p4.unwind_cycle = tag5.id 
left join ds_mysql_prod_tagelement tag6 
 on tag6.id = p4.unwind_product_category
 left join ds_mysql_prod_tagelement tag8
 on tag8.id = p2.unwind_details_new*/
 


