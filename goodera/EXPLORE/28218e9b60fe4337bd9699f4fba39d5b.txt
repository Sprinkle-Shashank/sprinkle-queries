with test1 as(
select concat(cast(fy_timerange.start as varchar), concat('|', cast(fy_timerange.end as varchar))) as fy_timerange, p.projectid, 'Yes' as govt, 'Trucker' as f_category,  p.tb_dbt_truckers as final_sum from profile_63887 p where final_sum is not null
  
  union all
  
select concat(cast(fy_timerange.start as varchar), concat('|', cast(fy_timerange.end as varchar))) as fy_timerange, p.projectid, 'Yes' as govt, 'Allied' as f_category, tb_allied as final_sum from profile_63887 p where final_sum is not null
)
select * from test1