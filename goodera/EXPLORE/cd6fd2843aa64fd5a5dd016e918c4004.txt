Select t.name as projectid,EXTRACT(YEAR FROM(CAST(fy_timerange.start as datetime))) as year, 
Sum(CASE 
             WHEN keyword = 'per_of_new_suppliers' THEN datanumBase
             ELSE 0 
           END) AS per_of_new_suppliers
from profile_79316
group by 1,2