SELECT M.name, sessions_attended,
S.name AS student,
M.fy_timerange,
cast(M.fy_timerange.start as date) as start,
cast(M.fy_timerange.end as date) as end,
DATEADD(year, 3, end) as end_new

FROM profile_56496 AS M

LEFT JOIN profile_56131 AS S
ON M.student = S._id