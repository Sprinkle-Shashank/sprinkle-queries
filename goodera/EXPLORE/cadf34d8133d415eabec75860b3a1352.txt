
select tag1.name as projectid, project_name, fy_timerange, budget_approved, budget_spent, beneficiaries, tag2.name as location, photo_before, photo_after, to_char(fy_timerange.start :: date, 'YYYY') as start_year, to_char(fy_timerange.end :: date , 'YYYY') as end_year, remarks, 'FY' || '(' || start_year || ' - ' || end_year || ')' as Financial_year, docs, partner
from profile_83767
left join ds_mysql_prod_project tag1 
on s1.projectid = tag1.id
left join ds_mysql_prod_tagelement tag2
on s1.location = tag2.id
group by 
tag1.name, project_name, fy_timerange, budget_approved, budget_spent, beneficiaries, tag2.name, remarks, docs, partner

