select p.projectId, p.batch_id, concat(cast(p.fy_timerange.start as varchar), concat('|', cast(p.fy_timerange.end as varchar))) as fy_timerange, p.placed ,h.start, h.end, h.donor, h.donor_commitment
from profile_36901 p

left join profile_36904 h on
p.batch_id=h._id	

where h.donor is not null

