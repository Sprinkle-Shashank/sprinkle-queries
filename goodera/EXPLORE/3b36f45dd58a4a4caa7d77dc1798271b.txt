with temp1 AS
(select m.projectId, m.indicator, m.sdgs, m.schedule_vii, m.project_name, m.ben_type, m.baseline, m.target, m.proj, r._id, r.fy_timerange, r.project_name, r.vertical, r.focus, r.state, r.direct_or_partner, r.project_code, r.start_date, site.name AS site_1
 
from profile_61044 AS m

left join profile_62881 AS r 
 on m.proj = r._id

left join ds_mysql_prod_tagelement as site
  on r.direct_or_partner = site.id)

select projectId, indicator, sdgs, schedule_vii, project_name, ben_type, baseline, target, proj, _id, project_name, vertical, focus, state, direct_or_partner, project_code, start_date, site.name AS site_1 from temp1 AS p

left join profile_55471 AS q
 on p.project_code = q.project_code


