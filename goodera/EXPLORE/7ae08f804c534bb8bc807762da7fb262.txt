with sub1 as(
select extract(month from cast(fy_timerange.start as date)) as month, fy_timerange, sessions_conducted, sessions_targetted, toilets_supposed, toilets_built from profile_80162)
select sum(sessions_conducted) as sessions_conducted, sum(sessions_targetted) as sessions_targetted, sum(toilets_supposed) as toilets_supposed, sum(toilets_built) as toilets_built, fy_timerange, month from sub1 group by fy_timerange, month