with s1 as(
Select t.name doc_type, upload,fy_timerange, file_name,p.name as projectid, 
  extract(month from cast(fy_timerange.start as datetime)) as month1,
  extract(year from cast(fy_timerange.start as datetime)) as year1,
   
  case
  when month1=1 then 'January'
  when month1=2 then 'February'
  when month1=3 then 'March'
  when month1=4 then 'April'
  when month1=5 then 'May'
  when month1=6 then 'June'
  when month1=7 then 'July'
  when month1=8 then 'August'
  when month1=9 then 'September'
  when month1=10 then 'October'
  when month1=11 then 'November'
  when month1=12 then 'December'
  end as monthh,


  (month1 || '-' || year1) as month2
  
  
  
from profile_79664 sq
   left join ds_mysql_prod_tagelement t
 ON t.id=sq.doc_type
  left join ds_mysql_prod_project p
  ON p.id=sq.projectid
  GROUP BY 1,2,3,4,p.name,6,7,8
)
 Select CAST(link as VARCHAR) as click_Here,'Click Here'as click,  doc_type, file_name,fy_timerange, projectid, month2
 from s1 a,a.upload link