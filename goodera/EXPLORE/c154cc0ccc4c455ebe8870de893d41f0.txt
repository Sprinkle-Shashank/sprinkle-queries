with s1 as (Select projectid, name,photo
FROM profile_79653 sq
)
Select name, projectid, CAST(photos_unwind AS VARCHAR)
from s1 a,a.photo photos_unwind
left join ds_mysql_prod_project p1
ON p1.id=a.projectid