select p.* , fac.name as factory_name, ven.name as vendor , cou.name as country  from profile_64075 p

left join ds_mysql_prod_project fac
on  p.projectid = fac.id

left join ds_mysql_prod_project ven
on fac.parent = ven.id

left join ds_mysql_prod_project cou
on ven.parent = cou.id

where (projectId <> 31303 and projectId <> 46455 and projectId <> 54832 and projectId <> 31299) and projectId ilike'%%'

/*

Fields: profileId, programId, organizationId, modified, created, submitted, geoLocation

*/