with sub1 as(
(SELECT sq.fy_timerange, sq.project_id
 --, pd.name_vendor_filter as vendor, pd.name_country as region, pd.partner_type 
 from profile_62491 sq
left join profile_62482 pd
on sq.project_id=pd.project_id)
  union all
  (select fy_timerange, program
   --, vendor, country, partner_type 
   from profile_62972))
  select * from sub1