
with table1 as (SELECT * 
FROM (SELECT male_new, female_new FROM profile_79767) UNPIVOT (
    new_admission FOR gender IN ( male_new, female_new)
)) 
select case when gender = 'male_new' then 'Male' else 'Female' end as gender,sum(new_admission) as new_admission from table1


group by gender
