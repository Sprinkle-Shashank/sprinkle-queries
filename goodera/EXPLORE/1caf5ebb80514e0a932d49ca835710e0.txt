select projectId.name as projectId, project_duration.name as project_duration, imple_partners, spoc_name, project_name, sched7, fy_timerange, budget_approved from profile_44749 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
left join ds_mysql_prod_tagelement as project_duration
on m.project_duration = project_duration.id