with sub1 as(select b.*, unwind_focus_area from profile_80634 b, b.focus_area as unwind_focus_area)
Select a.fy_timerange, p.name as projectid,p1.name as donor, a.funds_disbursed, listagg(t1.name, ', ') as focus_area	
from sub1 a
left join ds_mysql_prod_project p on p.id = a.projectid
left join ds_mysql_prod_project p1 on p1.id = p.parent
join profile_80499 b on b.projectid = a.projectid
left join ds_mysql_prod_tagelement t1 on t1.id=a.unwind_focus_area

where a.projectid like '%%'