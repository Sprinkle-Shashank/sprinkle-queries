Select projectId.name as projectId, state1.name as state,fy_1,projects, fy_timerange, count(*), teachers_trained, 'Education' as theme, 'Building Teacher Capacity' as Program from profile_51205 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
left join ds_mysql_prod_tagelement as state1
on m.state = state1.id

group by projectId.name , state1.name, fy_timerange, teachers_trained