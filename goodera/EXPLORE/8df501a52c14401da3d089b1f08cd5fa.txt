--select l._id,p.status from profile_54963 l left join Base1 p on l._id=p._id
with Base1 as(select m._id, o, cast(o."approvalStatus" as varchar) as status from test_hdfc_sample m, m.approvallog o ),Base2 as(select l._id,p.status from profile_54963 l left join Base1 p on l._id=p._id)
select * from Base2
