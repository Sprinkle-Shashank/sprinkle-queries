SELECT 
concat(concat(concat(split_part(reg_date, ' ', 1),'T'),split_part(reg_date, ' ', 2)),'.000Z') as fy_timerange,
search_status AS status,
COUNT(name) AS total

FROM profile_59462

GROUP BY fy_timerange, status, total