select p1.projectId, p1.date as fy_timerange, p1.project_name, p1.theme_session, p1.type_session, p1.area_name, p1.platform, p5.theme,p6.imp_partner, p6.bu, p3.name as location, sum(p1.no_benf) as no_benf, sum(p1.no_areas) as no_areas, count(*) as no_sessions, sum(p1.no_hrs) as no_seshrs, sum(p1.ntp_bags) as ntp_bags, sum(p1.cl_waste) as area_covered_in_waste_collection, sum(cl_areacleaned) as area_cleaned, sum(cl_areapainted) as area_painted, sum(cl_wastecollect) as cl_waste, sum(wb_bins) as wb_bins, 
sum(wb_wetwaste) as wb_wetwaste, sum(wb_compost) as wb_compost, sum(no_volunteers) as no_volunteers
from profile_71835 p1
left join ds_mysql_prod_project p2
on p2.id = p1.projectId
left join ds_mysql_prod_project p3
on p3.id = p2.parent
left join ds_mysql_prod_project p4
on p4.id = p3.parent
left join profile_72050 p5
on p1.projectId = p5.projectId
left join profile_71728 p6
on p1.project_name = p6._id
group by p1.projectId, p1.date , p1.project_name, p1.theme_session, p1.type_session, p1.area_name, p1.platform, p5.theme, p6.imp_partner, p6.bu, p3.name 