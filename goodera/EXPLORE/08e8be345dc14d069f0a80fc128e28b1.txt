select p.projectid as project_id, t.name as projectname, p.name as allocated
from profile_66076 p
left join ds_mysql_prod_project as t
on p.projectid = t.id
left join ds_mysql_prod_tagelement as t
on p.allocated = t.id
where p.projectid = 61203
















