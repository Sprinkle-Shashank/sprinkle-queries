Select projectid,fy_timerange,title_app,students_involved,teachers_involved,report,photos,fy_timerange.start as qs,fy_timerange.end as qe,SUBSTRING((CAST(qs.start AS datetime)),1,7) as Month from profile_78794


/*SUBSTRING((CAST(p.fy_timerange.start AS datetime)),1,7) as Month*/
/*Select projectid,fy_timerange,to_char(profile_78793.date_session, 'DD-MM-YYYY') as date_session,
type.name as type_topic,
name_topic,desc_topic
from profile_78793 --select stage
left join ds_mysql_prod_tagelement type --retrieve element from id
 on profile_78793.type_topic = type.id
GROUP BY projectid,fy_timerange , date_session , type.name , name_topic,desc_topic  --group by*/