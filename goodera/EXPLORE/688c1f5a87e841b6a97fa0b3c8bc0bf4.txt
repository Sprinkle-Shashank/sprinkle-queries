--with t1 as (
  select p.name as projectId, vendor, p2.name as parent,consultant_tag,consultant, tag1.name as training_type, tag3.name as cycle ,tag4.name as country from  profile_73974 as p1
 left join ds_mysql_prod_project p
on p.id = p1.projectId
left join ds_mysql_prod_project p2 
on p2.id = p.parent
left join ds_mysql_prod_project p3
on p3.id = p2.parent

left join ds_mysql_prod_tagelement tag2
on p1.consultant_tag = tag2.id
left join ds_mysql_prod_tagelement tag1
on p1.training_type = tag1.id
left join ds_mysql_prod_tagelement tag3
on p1.cycle  = tag3.id
left join ds_mysql_prod_tagelement tag4
on p1.country = tag4.id
--where tag is null
--)


--select projectId , country,vendor, consultant, training_type,cycle, split_to_array(parent,'-') from t1















