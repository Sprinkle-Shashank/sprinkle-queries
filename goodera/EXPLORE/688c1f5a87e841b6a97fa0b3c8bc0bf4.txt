
 with t1 as 
(
  select p3.name  as projectId , timerange_timerange,  tag1.name as training_type, num_female, num_male, tag2.name as  year , tag4.name as country, p3.factory, p2.vendor, p2.cycle, p2.product_category,tag6.name as consultant 
from profile_61528 as p1
left join profile_64362 as p2
on p1.projectId = p2.projectId
left join ds_mysql_prod_project p3
on p1.projectId= p3.id
  left join profile_64170 as p3
on p2.factory = p3._id
left join ds_mysql_prod_tagelement tag1
on p1.training_type = tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.year = tag2.id

left join ds_mysql_prod_tagelement tag4
on p2.country = tag4.id
left join ds_mysql_prod_tagelement tag6
on p2.consultant_tag = tag6.id
--left join ds_mysql_prod_tagelement tag7
--on p2.unwind_product_category = tag7.id 
--order by timerange_timerange DESC
 ),

 sub2  as(select a.*, unwind_cycle from sub1 a,a.cycle as unwind_cycle)
 
t2 as( select c.projectId, c.timerange_timerange,  c.training_type, c.num_female, c.num_male, c.year ,c.country, c.factory, c.vendor, c.product_category,c.consultant ,tag3.name as cycle, row_number() over (partition by c.projectid,tag3.name,c.country,c.vendor,c.consultant order by cast (c.timerange_timerange.start as DATE) DESC ) as row
	  from sub2 c
left join ds_mysql_prod_tagelement tag3
on c.unwind_cycle = tag3.id 
		  








