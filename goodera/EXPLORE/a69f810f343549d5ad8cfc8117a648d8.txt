SELECT projectId, the.name AS theme, fy_timerange, amount, pro.name, 
EXTRACT(YEAR FROM p.fy_timerange.start)

AS projects FROM profile_81326 p

LEFT JOIN tagElement_4886 pro
ON p.projects = pro.id

LEFT JOIN tagElement_4886 the
ON p.theme = the.id

--tagElement_4886