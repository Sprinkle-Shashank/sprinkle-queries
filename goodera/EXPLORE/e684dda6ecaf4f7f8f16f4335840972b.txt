with table1 as (Select 
				JSON_SERIALIZE(table_52810.pace_brands) as pace_brand,	
				case when pace_brand like '%1011796%' then 1 else 0 end  as brand,
				reverse(SPLIT_PART(reverse(projcode_factory), '-',1)) as code_factory,
				cast(projid_country as int) as countryId,
				projid_rp as rpId,
				cast (projid_vendor as int) as vendorId,
				projcode_factory as factoryCode,
				name_factory as factoryName,
				
				projid_factory as factoryId,
				case when table_52810.offset_date is null then '2020-02-01T00:00:00.000Z' else table_52810.offset_date end as offset_date,
				table_4227.start_date as start_date1,
				case when start_date1 >= offset_date then 1 else 0 end as datestatus
				
				
				from profile_54646 p
				
inner join profile_52810 table_52810 on
table_52810.projectId = p.projectId

left join profile_4227 table_4227 on
table_4227.projectId = p.projectId
	
),

table2 as (Select 
		   brand, 
		   factoryId, 
		   listagg(distinct code_factory, ',') as code_factory, 
		   listagg(distinct countryId, ',') as countryId, 
		   listagg(distinct rpId , ',') as rpId, 
		   listagg(distinct vendorId , ',') as vendorId, 
		   listagg(distinct factoryCode ,',') as factoryCode, 
		   listagg(distinct factoryName , ',') as factoryName,datestatus
		from table1 p
group by brand,factoryId,datestatus

)


select * from table1 p  where factoryname = 'Factory: Sumbiri Hela Intimates PLC'
--where brand = 1 and datestatus = 1 and code_factory = 'PVH'