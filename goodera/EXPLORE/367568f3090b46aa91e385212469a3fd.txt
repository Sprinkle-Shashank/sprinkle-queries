with table12 as
 ( 
  select p.projectid , p._id , status from profile_16619 as p
 ),
 
 table14 as
 (
 select p.projectid , p._id , p.d1_update ,  ROW_NUMBER() over (partition by p.projectid order by p._id desc) as row, 1 as count , sta.name as status_update from table12 as p
   
   
left join ds_mysql_prod_tagelement sta
on p.status = sta.id
   
),

table15 as
( select p.projectid , p._id , status_update from table14 as p where row=1 ),

table18 as
(
  select p.projectid , last_day_workers , workers_exited , day_1_workers from profile_16862 as p)

select * from table15
