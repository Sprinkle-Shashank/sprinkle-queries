select projectId, state, school_name, photo_pre_construction, photo_post_construction, fy_timerange, work_type, students_reach, class_const, 
case
when photo_pre_construction is Not NULL then 'Download' else '-'
end as pre_photo,
case
when photo_post_construction is Not NULL then 'Download' else '-'
end as post_photo
from profile_51204 as m
order by fy_timerange desc

