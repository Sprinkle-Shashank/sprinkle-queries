with table1 as (
select sp.name as species, sum(no_of_trees_each_specie) as trees_palnted from profile_79496

left join ds_mysql_prod_tagelement sp
on profile_79496.species = sp.id
  
  group by species
  
  
)
select * from table1
--select * from profile_79497