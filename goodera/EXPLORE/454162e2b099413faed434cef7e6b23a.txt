select p1.fy_timerange, sum(no_teachers_trained)as no_teachers_trained,count(p1._id) as session_count,
case 
when p1.duration_training  month< 4 then 'Q4'
when p1.duration_training month < 7 then 'Q1'
when p1.duration_training  month< 10 then 'Q2'
else 'Q3'
end as quater 
from profile_76013 p1
group by p1.fy_timerange,p1._id,p1.date_training, p1.no_teachers_trained,p1.duration_training


--if else condition is not clear 