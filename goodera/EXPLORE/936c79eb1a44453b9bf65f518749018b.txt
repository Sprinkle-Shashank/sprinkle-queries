with table1 as (
  select subject from p.subjects as subject
  )
select p._id, p.teacher_name, t.name, count(*) 
from profile_77379 p 
left join ds_mysql_prod_tagelement t
on table1.subject = t.id
group by 1,2,3;