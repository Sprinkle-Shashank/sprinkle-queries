with temp1 as
(select m.projectId, m.project_name, m.vertical, m.focus, m.start_date, m.project_code, m.entity, r.completed, r.fy_timerange AS fy_timerange_new, r.progress
 
from profile_62881 AS m 

left join profile_55471 AS r 
 on m.project_code = r.project_code)
 
select m.projectId, m.project_name, m.vertical, m.focus, m.start_date, m.project_code, m.entity, m.completed, m.fy_timerange_new, m.progress from temp1 as m,

LISTAGG(CAST(fy_timerange_new.end AS date),',') AS update_date
