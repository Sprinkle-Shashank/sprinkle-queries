SELECT 
--EXTRACT(YEAR FROM(CAST(month_timerange.start AS datetime))) AS mylan_year,
--CAST(month_timerange.start AS datetime) AS timeperiod_start,

CASE 
  WHEN (EXTRACT(month from month_timerange.start) <4)
  THEN (EXTRACT(year from month_timerange.start)-1)
  ELSE  (EXTRACT(year from month_timerange.start))
  END AS fy_timerange
  
FROM profile_55194