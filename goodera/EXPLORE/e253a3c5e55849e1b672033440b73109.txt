SELECT 
--EXTRACT(YEAR FROM(CAST(month_timerange.start AS datetime))) AS mylan_year,
--CAST(month_timerange.start AS datetime) AS timeperiod_start,

CASE 
  WHEN (EXTRACT(month from(CAST (month_timerange.start AS datetime)) <4))
  THEN (EXTRACT(year from (CAST (month_timerange.start AS datetime))-1))
  ELSE  (EXTRACT(year from (CAST (month_timerange.start AS datetime))))
  END AS fy_timerange
  
FROM profile_55194