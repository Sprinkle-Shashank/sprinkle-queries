select projectId, p.date as fy_timerange, remarks, receiver, type, variant, unit, amount_per_unit, reports,
 unit*amount_per_unit as total_amount ,CONCAT(
  CAST(DATE_PART('day', p.date) AS VARCHAR(2)), 
  '-',
  CAST(DATE_PART('month', p.date) AS VARCHAR(2)), 
  '-',
  CAST(DATE_PART('year', p.date) AS VARCHAR(4))
)
from profile_74557 p

