with t1 as
(
  select p.projectid, pr.name as projectid_updated, allocated, fy_timerange 
  from profile_56357 as p
  
  left join ds_mysql_prod_tagelement pr
  on p.projectid = pr.id
  
  ),
  
  t2 as
  (
	select projectid, fy_timerange, sum(allocated) as Allocated_amount_for_this_FY from t1
   
	group by projectid, fy_timerange
	)
	
	select * from t2
	