select p1._id, p2.name as projectId, centre, t1.name as type, fy_timerange, meal, target, date, to_char(date, 'DD-MM-YYYY') as new_date, round((target/meal)*100,2) as val1,  concat (val1,'%') as per

from profile_76422 p1
left join ds_mysql_prod_project p2 on p1.projectid = p2.id
left join ds_mysql_prod_tagelement t1 on p1.type= t1.id