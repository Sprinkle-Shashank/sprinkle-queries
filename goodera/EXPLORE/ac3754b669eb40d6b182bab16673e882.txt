select p1.name as vendor , cou.name as country , 
start_year1.name as start_year

--New Field: statusTag/Project Field: statusAttribute: tagElement

--New Field: service_providerTag/Project Field: service_providerAttribute: tagElement

--New Field: featuresTag/Project Field: featuresAttribute: tagElement

from profile_60670 p

left join ds_mysql_prod_project fac
on  p.projectid = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id
left join ds_mysql_prod_project cou
on ven.parent = cou.id

left join profile_54646 p1
on p.projectId = p1.projectId

left join ds_mysql_prod_tagelement start_year1 on
	p.start_year = start_year1.id