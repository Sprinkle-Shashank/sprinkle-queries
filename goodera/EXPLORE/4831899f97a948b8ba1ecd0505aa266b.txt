select p1.name as projectId, p3.name as milestone, p_start, p_end, a_start, a_end, delay, nvl(getdate(),a_end) as new_a_end,p_start as fy_timerange

from profile_76385
left join ds_mysql_prod_project p1 on p1.id=projectId
left join profile_76368 p3 on p3._id= milestone
group by p1.name, fy_timerange,p3.name

