Select timerange_timerange as fy_timerange , p.name as projectid,p1.name as Implementation_partner,t.name as fund,amount

from profile_58321 a
where fund = 'Allocated';


left join ds_mysql_prod_project p on p.id = a.projectid
left join ds_mysql_prod_project p1 on p1.id = p.parent

left join ds_mysql_prod_tagelement t on t.id=a.fund




