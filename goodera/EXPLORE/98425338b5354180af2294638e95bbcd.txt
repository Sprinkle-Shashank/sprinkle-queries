select city1.name as city1, state1.name as state1, fy_timerange, school, action_plan, training, students, family_member, pre_awareness, post_awareness, fy_timerange.start as start1,Start1.month as Month_up,CONCAT(CONCAT(city1,'-'),state1) AS location from profile_51222 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
left join ds_mysql_prod_tagelement as state1
on m.state = state1.id
left join ds_mysql_prod_tagelement as city1
on m.city = city1.id