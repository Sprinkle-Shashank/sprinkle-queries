with sub1 as(
  select add_up from profile_78780 p1, p1.add_up_2 add_up)
  
select distinct t1.name as add_up from sub1 s1
left join ds_mysql_prod_tagelement t1 on t1.id = s1.add_up
where t1.name not in ('Behaviour', 'Knowledge', 'Nowhere')