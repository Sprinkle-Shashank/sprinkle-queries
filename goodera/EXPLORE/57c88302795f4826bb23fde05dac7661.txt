with sub1 as(
  select *, focus_area from profile_80399 p1, p1.thurst_area focus_area)
  
select s1.csr_vision_mission, s1.logo, listagg(distinct t1.name, ', ') as focus_area
from sub1 s1
left join ds_mysql_prod_tagelement t1 on t1.id = s1.focus_area