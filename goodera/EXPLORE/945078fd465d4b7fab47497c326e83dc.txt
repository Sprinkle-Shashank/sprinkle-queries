select projectId, service_provider, status, year_timerange, other_status, start_year, survey_yn, ven.name as vendor, cou.name as country, yr.name as year from profile_60670 p

left join ds_mysql_prod_project fac
on p.projectid = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id
left join ds_mysql_prod_project cou
on ven.parent = cou.id

left join ds_mysql_prod_project yr
on start_year.id = yr.id


/*
left join ds_mysql_prod_tagelement ven
on profile_60670.projectid = ven.id
left join ds_mysql_prod_tagelement cnt
on prnme.parent = cnt.id

left join ds_mysql_prod_tagelement vendor
on p.location = vendor.id
left join ds_mysql_prod_tagelement sta
on cit.parent = sta.id
left join ds_mysql_prod_tagelement cou
on sta.parent = cou.id;
group by projectId, year_timerange, year, wep_status, service_provider, survey_yn, vendor, country

where (country != 13761) and (wep_status == 'Active')

*/

