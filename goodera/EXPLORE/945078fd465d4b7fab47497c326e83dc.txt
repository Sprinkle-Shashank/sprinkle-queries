select projectId, service_provider, status, year_timerange, other_status, start_year, survey_yn, 
ven.name as vendor, country.name as country from profile_60670

left join ds_mysql_prod_tagelement prnme
on profile_60670.projectid = prnme.id
left join ds_mysql_prod_tagelement cnt
on prnme.parent = cnt.id
left join ds_mysql_prod_tagelement ven
on cnt.parent=ven.id

