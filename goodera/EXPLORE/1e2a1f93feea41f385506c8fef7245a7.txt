select name_ben, city,"desc",  photo, fy_timerange,pro.name as projectId,parent1.name as parent1 from profile_56086 p
 left join ds_mysql_prod_project as pro
            on p.projectid = pro.id 
left join ds_mysql_prod_project as parent1
            on pro.id = parent1.id 			
