Select count(p.validator) as Task_Validated, count(p.concern_level) as Concern_Raised,t.name fy,p.village_name
FROM profile_27412 p
left join ds_mysql_prod_tagelement t
on p.fy=t.id
group by t.name,p.village_name
PIVOT (
  count(Concern_Raised)
  for (Task_Validated)
  )