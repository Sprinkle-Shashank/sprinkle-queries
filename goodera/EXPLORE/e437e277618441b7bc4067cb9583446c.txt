select * from taskresp_54963

/*with NS AS (
  select 1 as n union all
  select 2 union all
  select 3 union all
  select 4
)
select
  TRIM(SPLIT_PART(t.approvallog, ',', NS.n)) AS approval
from NS
inner join taskresp_27396 t ON NS.n <= REGEXP_COUNT(t.approvallog, ',') + 1*/
