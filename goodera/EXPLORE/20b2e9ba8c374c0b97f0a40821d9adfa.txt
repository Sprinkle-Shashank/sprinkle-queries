select * from profile_56356
/*select projectId.name as projectId, line_item, date as fy_timerange, SUM(amount_utilized) as Total_Amount_Utilized, direct_indirect
from profile_56356 as m

left join profile_56355 AS r
 	on m.line_item = r._id
	
left join ds_mysql_prod_project as projectId
  on m.projectId = projectId.id
  
group by projectId.name, line_item, fy_timerange, direct_indirect*/
