SELECT projectId, year, t.name AS quarter, timerange_timerange, SUM(amount_utilized) AS Utilized, SUM(amount_rvd) AS Approved
FROM profile_3537
left join  ds_mysql_prod_tagelement t  on
t.id=quarter
GROUP BY projectId, year, quarter, timerange_timerange, amount_utilized, amount_rvd
ORDER BY quarter DESC
