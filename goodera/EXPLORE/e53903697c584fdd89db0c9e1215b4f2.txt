select concat(concat(substring(registration_date,1,4),'-'),cast(substring(registration_date,1,4) as int)+1) as fy_timerange,count(*) as Number,
case 
when fy_timerange is not null then 'New Due Diligence' 
else 'NA' 
end as Category
from profile_80188 p 
group by registration_date