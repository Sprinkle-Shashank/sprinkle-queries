with sub1 as(
  select p1.*, sdgs from profile_73154 p1, p1.sdg_new sdgs),
  
sub2 as(
  select sdgs from sub1 group by sdgs)
  
select substring(t1.name, 4, (len(t1.name) - 3)) as sdg, p2.sdg_icon
from sub2 s2

left join profile_80456 p2 on p2._id = s1.sdgs