select pro.name as projectid,fy_timerange,stat.name as state,govt_agency,phc_identified,phc_granted,donation from profile_79422 p

left join ds_mysql_prod_tagelement as stat 
  on p.state = stat.id
  
  left join ds_mysql_prod_project as pro
  on p.projectid = pro.id