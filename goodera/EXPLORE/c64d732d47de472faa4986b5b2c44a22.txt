with t1 as
(
  select _id, projectId, date_invoice, invoice_doc, date_disbursal, invoice_amount, disbursed, receipt
  from profile_56364 as p
  
  where invoice_doc<>''
  ),
  t2 as
  (
	select _id, projectId, date_invoice as fy_timerange, invoice_doc, date_disbursal, invoice_amount, disbursed, receipt, substring(date_disbursal,1,10) as Disbursal_Date, substring(date_invoice,1,10) as Invoice_Date,       'Invoice' as Invoice, 'Disbursal Receipt' as Disbursal_Receipt from t1
	
	order by fy_timerange DESC
	)
	select * from t2
	
  