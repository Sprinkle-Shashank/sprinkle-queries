with table1 as 
( select  p.projectid , p._id , fy_timerange , cast(fy_timerange.start as date) as year_start , extract(year from year_start) as year_start_update , (year_start_update+1) as year_start_update_new , substring(year_start_update_new,3,2) as end_new , concat(year_start_update,'-') as ca1 , concat(ca1,end_new) as ca2 , sch_na.name as school_name_update , sta.name as state_update , ci.name as city_update , s_cons.name as school_constr_update , fur.name as furn_update , s_type.name as school_type_update , w_type.name as work_type_update , the.name as theme_update , mon.name as month_update , num_stu , photo , case when is_array(photo) = false or get_array_length(photo) = 0 then array(photo)
else (photo) end as photo_update from profile_83063 as p

left join ds_mysql_prod_tagelement sch_na 
on p.school_name = sch_na.id

left join ds_mysql_prod_tagelement sta 
on p.state = sta.id
 
left join ds_mysql_prod_tagelement ci 
on p.city = ci.id

left join ds_mysql_prod_tagelement s_cons 
on p.school_constr = s_cons.id
 
left join ds_mysql_prod_tagelement fur 
on p.furn = fur.id

left join ds_mysql_prod_tagelement s_type 
on p.school_type = s_type.id

left join ds_mysql_prod_tagelement w_type 
on p.work_type = w_type.id
 
left join ds_mysql_prod_tagelement the 
on p.theme = the.id

left join ds_mysql_prod_tagelement mon 
on p.month = mon.id
),

table2 as
(
  select *  from table1 as p
  ), 
  
 table3 as 
 ( select * from table2 as p, p.photo as d1),
 
 table4 as
 (
   select * from table3 as p)
   
select * from table3