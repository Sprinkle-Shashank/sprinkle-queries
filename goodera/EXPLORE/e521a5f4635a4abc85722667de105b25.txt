with sub1 as (
select  rtrim(ltrim(json_serialize(sched7),'['),']') as sched7, bene_num, fy_timerange from profile_41363 
  where bene_num is not null),
sub2 as (
  select fy_timerange, bene_num, sched7 from sub1 where sched7 is not null)
	select t.name as sched7, bene_num, fy_timerange from sub2
   left join ds_mysql_prod_tagelement t 
   on t.id=sched7
	