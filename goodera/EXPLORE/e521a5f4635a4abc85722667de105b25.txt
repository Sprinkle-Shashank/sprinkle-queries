with sub1 as (
select  rtrim(ltrim(json_serialize(sched7),'['),']') as sched7, bene_num, fy_timerange from profile_41363 
  --left join ds_mysql_prod_tagelement t 
  --on t.id=sched7
  where bene_num is not null)
  /*,
sub2 as (
  select fy_timerange, bene_num, listagg(distinct json_serialize(sched7), ', ') as Schedule_7 from sub1 group by fy_timerange, bene_num),
  sub3 as(
  select fy_timerange, bene_num, Schedule_7_1 as Schedule_7 from sub2 a, a.Schedule_7 Schedule_7_1)*/
  select * from sub1