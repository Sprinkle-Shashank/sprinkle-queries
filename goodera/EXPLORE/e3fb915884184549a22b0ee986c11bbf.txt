Select pid.id as project_id, p.director_region, ven.name AS Vendor, cou.name as country,fac.name AS factory


from profile_76755 p
left join ds_mysql_prod_project fac
on p.projectId = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id

left join ds_mysql_prod_project cou
on ven.parent = cou.id
left join ds_mysql_prod_project pid
on cou.parent = pid.id

left join ds_mysql_prod_tagelement dir
on p.director_region = dir.id




group by cou.name,p.director_region,ven.name,pid.d,fac.name
order by director_region, cou.name,ven.name,pid.id asc