select  distinct split_part(split_part(prod.name,'|',1),'.',1) as projectcode,
f.name as financial_year,
concat(projectcode,financial_year) as CODE
from profile_27396 p1
left join ds_mysql_prod_project prod
on prod.id=p1.projectid
left join ds_mysql_prod_tagelement f on
p1.fy=f.id