Select t.name as site,t1.name as sub_Region,t2.name as Region,
	  
	  
		   Sum(CASE 
             WHEN keyword = 'propane_usage' THEN datanumBase
             ELSE 0 
           END) AS prop_usage
		  
		   from sus_56999 
  left join ds_mysql_prod_project t
  on xprojectid=t.id
  left join ds_mysql_prod_project t1
  on t.parent=t1.id
  left join ds_mysql_prod_project t2
  on t1.parent=t2.id
 where keyword ='propane_usage'
  group by t.name,t1.name,t2.name