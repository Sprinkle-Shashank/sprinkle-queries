select p.projectId,cou.name as country, ven.name as vendor,p.production_efficiency,p1.production_efficiency_pre,p1.employee_id_pre,(p.production_efficiency-p1.production_efficiency_pre) as difference,case when difference>0
then
1
else 
0 
end as count_increase
from profile_65282 p


left join profile_60669 p1
on p.employee_id = p1.employee_id_pre

left join ds_mysql_prod_project fac
on  p.projectid = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id
left join ds_mysql_prod_project cou
on ven.parent = cou.id




group by p.projectId, cou.name, ven.name,p1.production_efficiency_pre,p1.employee_id_pre,difference,count_increase,production_efficiency