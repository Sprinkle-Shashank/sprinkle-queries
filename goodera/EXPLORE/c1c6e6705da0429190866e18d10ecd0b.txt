with s1 as(
  Select description, upload_file, photo, fy_timerange, q.name event_type, p.name projectid
FROM profile_80084 sq
left join ds_mysql_prod_project p
oN p.id=sq.projectid
left join ds_mysql_prod_tagelement q
ON q.id=sq.event_type
GROUP BY 1,2,3,4,5,6
  )
  Select link as 'Click Here', description,photo, fy_timerange,event_type,Click_Here as Click,projectid
  FROM s1 a, a.upload_file link