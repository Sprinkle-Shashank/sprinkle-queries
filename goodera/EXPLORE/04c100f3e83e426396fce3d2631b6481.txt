with table1 as
(
  select projectId, files, 'desc' as desc_update, time_timerange, name, 'View Files' as files1, extract(year from CAST(p.time_timerange.start AS date)) AS start_1, extract(year from CAST(p.time_timerange.end AS date)) AS end_1
  
  from profile_62810 as p
  ),
  
  table2 as
  (
	select  projectId, files, desc_update, time_timerange, files1, start_1,end_1, concat('FY ',concat(start_1,concat(' - ',substring(end_1,3,2)))) as name_update_1 from table1
	)
  
  select projectId, files, desc_update, time_timerange, files1, start_1,end_1,name_update_1 from table2