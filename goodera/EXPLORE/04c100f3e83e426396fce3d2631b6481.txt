with table1 as
(
  select projectId, files, 'desc' as desc_update, time_timerange, name, 'View Files' as files1
  
  from Profile_62810 as p
  ),
  
  table2 as 
  (select projectId, files, desc_update, time_timerange, name, files1, CAST(tie