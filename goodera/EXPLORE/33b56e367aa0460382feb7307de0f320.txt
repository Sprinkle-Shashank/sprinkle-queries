select tag1.name as projectid, fy_timerange, applications_selected, applications_rejected, applications_selected + applications_rejected as total_applications
from profile_83669 p1
left join ds_mysql_prod_project tag1
on p1.projectid = tag1.id