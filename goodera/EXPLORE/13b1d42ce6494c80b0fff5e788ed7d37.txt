select sum(migrant_workers) from profile_60928 
left join tagElement_7200 t2 on t2.id = curr_cr_rating
where migrant_workers is not null and name like 'Feb%' and t2.name not in ('Gray', 'White', 'Red', 'No Rating', 'Pending Rating', 'Deactivated')
/*with sub1 as (
  select p1._id as id, p1.quarter_timerange as fy_timerange, p2.name as factory, p1.factory_vms_id, nvl(t2.name, t1.name) as region, p3.name as vendor, p1.vendor_vms_id, p1.meet_target,  p1.cdms_id,
extract(year from cast(p1.quarter_timerange.start as date)) as start_year,  

dense_rank() over(partition by p2.name, p1.factory_vms_id, p3.name, p1.vendor_vms_id, start_year order by cast(p1.quarter_timerange.start as DATE) desc) as row

from profile_66533 p1
left join profile_81458 p2 on p2._id = p1.factory_new
left join profile_81524 p3 on p3._id = p1.vendor
  
left join tagElement_7200 t1 on t1.id = p2.factory_country
left join tagElement_7200 t2 on t2.id = t1.parent
where p2.name is not null and nvl(p2.factory_no,0) != 995995 and nvl(p3.vendor_no,0) != 995995 and start_year = 2022),

sub2 as(
  select * from sub1 where row = 1),
  
sub3 as(
  select id, cdms_id, factory, factory_vms_id, vendor, vendor_vms_id, region, 1 as count
from sub2
where meet_target = 'Yes'
group by id, factory, factory_vms_id, vendor, vendor_vms_id, region, cdms_id),

---x---

sub4 as(
  select s3.id, s3.factory, s3.vendor, s3.vendor_vms_id, p4.source_div, p4.timeperiod_timerange, s3.factory_vms_id,
  
case
  	when IS_ARRAY(p4.source_div) = FALSE or GET_ARRAY_Length(p4.source_div) = 0 then ARRAY (p4.source_div)
  	else p4.source_div
end as source_div_new,  
  
row_number() over(partition by s3.id order by cast(p4.timeperiod_timerange.start as date) desc) as row

from sub3 s3
left join profile_60928 p4 on p4.factory_id = s3.cdms_id),

sub5 as(
  select * from sub4 where row = 1)
  
select * from sub5*/