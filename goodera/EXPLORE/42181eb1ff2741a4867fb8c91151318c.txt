select projectId, term, CONCAT(SUBSTRING(CAST(event_date as date),9,2),SUBSTRING(CAST(event_date as date),6,2),SUBSTRING(CAST(event_date as date),1,4)) as datee,event_date as fy_timerange, p.location, objective, brief, outcome, attendees from profile_65295 p

