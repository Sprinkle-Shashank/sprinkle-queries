select tag1.name as projectid, student_name, roll_number, course, fy_timerange, join_scholarship_year, scholarship_amount, _id
from profile_64048 p1
left join ds_mysql_prod_project tag1
on p1.projectid = tag1.id