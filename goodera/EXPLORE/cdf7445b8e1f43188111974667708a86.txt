--Novartis_Migration_in_Leprosy_Patients_Timelines

with t1 as
(
  select projectId, phase_milestone, subact, p_s, p_e, a_s, a_e, status
  from profile_76421 as p
  ),
  t2 as
  (
	select concat(day(p_s),concat('-',concat(p.month(p_s),concat('-',concat(year(p_s),concat('to',concat(day(p_e),concat('-',concat(month(p_e),concat('-',concat(year(p_e)))))))))))) as proposed_duration from t1
											 )
								  select proposed_duration from t2
								  
left join ds_mysql_prod_tagelement t3 on t3.id=phase_milestone
left join ds_mysql_prod_tagelement t4 on t4.id=t3.parent