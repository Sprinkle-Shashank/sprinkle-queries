select p.projectid , p._id , fac.id as factory_no , fac.name as factory , ven.id as vender_id , ven.name as vender
from profile_61483 as p

left join ds_mysql_prod_project fac on
p.projectid = fac.id

left join ds_mysql_prod_project ven on
fac.parent = ven.id

/*select
id, profileId, programId, organizationId, geoLocation*/