select p.projectid , p._id , fac.name as country1 , country1 as country2 , case when country2 is null then p.projectid else country2 end as country
from profile_61483 as p

 
left join ds_mysql_prod_project fac   
on  p.projectid = fac.id             



/*select
id, profileId, programId, organizationId, geoLocation*/