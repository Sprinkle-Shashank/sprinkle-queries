Select sq.projectId, sq.fy_timerange, sq.student_name, sq.class, sq.gender, sq.school_name, sq.student_id, sq.student_age, t5.name as location, sq.age_bucket,
case 
	when 1=1 then 1
	else 0
	end as count
FROM profile_74884 sq
LEFT JOIN profile_74885 pd
ON sq.school_name=pd._id 
left join ds_mysql_prod_tagelement t5
on sq.location = t5.id

GROUP BY projectId, fy_timerange, student_name, class, gender, school_name, student_id, student_age