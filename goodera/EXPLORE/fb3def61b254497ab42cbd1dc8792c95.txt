select cast(p.features.name as varchar) as features_1,
listagg(features_1 , ',') as features_begin
from profile_65025 p
  
 left join ds_mysql_prod_tagelement features1 on
   p.features = features1.id
	
	
	group by p.features