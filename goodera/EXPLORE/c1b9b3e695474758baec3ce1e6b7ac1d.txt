with table1 as ( select distinct projectid,fac.name as factory,trainer_org from profile_64075 p

left join ds_mysql_prod_project fac
on p.projectid = fac.id
			   
	where trainer_org not ilike '%facility%'		   )

select distinct projectid,trainer_org from profile_64075
where trainer_org ilike '%facility%' and projectid not in (select projectid from table1)
