SELECT projectid, fy_timerange, calculation,
SUM(percent2) AS percent2,
SUM(surplus) AS surplus,
SUM(amount) AS amount,
SUM (total_csr) AS total_csr
FROM profile_78683
GROUP BY projectid, fy_timerange, calculation