with sub1 as (select a.*, unwind_cycle from profile_64816 as a, a.cycle as unwind_cycle)

select p1._id, p1.projectId, p1.evident, p1.status, p1.training_type, p1.year, p1.start_date_2,p5.country, p5.vendor, p5.product_category, p5.consultant, p5.cycle,unwind_cycle
from sub1 p1 
left join ds_mysql_prod_project p2
on p2.id = p1.projectId
left join ds_mysql_prod_project p3
on p3.id = p2.parent
left join ds_mysql_prod_project p4
on p4.id = p3.parent
left join profile_64362 p5
on p1.projectId=p5.projectId
left join ds_mysql_prod_tagelement tag1
on p5.cycle = tag1.id