select a.projectid,a.year,a.city, sum(a.total_employees) as employees, b.name as year_text
from profile_60438 a
left join ds_mysql_prod_tagelement b on a.year = b.id
group by a.year,a.city,b.name, a.projectid