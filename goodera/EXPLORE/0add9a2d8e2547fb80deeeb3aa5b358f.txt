select projectId, fy_timerange, school_name, school_location, latitude, ROUND(longitude,4) as longitude ,t2.name as state,count(*) from profile_75354 p


left join ds_mysql_prod_tagelement t on t.id=p.school_location
left join ds_mysql_prod_tagelement t2 on t2.id=t.parent

Group By projectId, fy_timerange, school_name, school_location, latitude, longitude, t2.name