select a.projectid, a.timerange_timerange as fy_timerange, t1.name as indicator, sum(a.value) from profile_55744 a 

left join ds_mysql_prod_tagelement t1 on t1.id = a.nanhi_kali_indicators
where indicator = 'No of Schools Covered'
group by a.projectid, a.timerange_timerange, t1.name

