select projectid,stat.name as status,train.name as training_type,yr.name as year,name_rep from profile_64806 p

left join ds_mysql_prod_tagelement stat
on p.status = stat.id

left join ds_mysql_prod_tagelement train
on p.training_type = train.id

left join ds_mysql_prod_tagelement yr
on p.year = yr.id

--where train.name = 'Left the committee'
