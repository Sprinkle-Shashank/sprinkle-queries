SELECT date_join as fy_timerange, name, Decode(graduated,true,'YES','NO') as graduated,Decode(dropout,true,'YES','NO') as dropout, transfer, 1 as count

FROM profile_79758

GROUP BY date_join, name, graduated, dropout, transfer, count