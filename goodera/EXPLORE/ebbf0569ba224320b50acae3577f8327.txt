with table1 as
( select p.projectid , p._id , fy_timerange , ng.name as ngo_update , sta.name as state_update , ci.name as city_update , description , programId, organizationId, modified, created, submitted, story_pic_1, story_2, story_description_1, story_description_2, sdg_goal, project_pic, name, projects, fy_1, project_status, theme, description, sdg_goal_number, project_pic_link, year_of_inception, stories, target_students, target_schools, beneficiaries
 from profile_51331 as p

left join ds_mysql_prod_tagelement ng
on p.ngo= ng.id
 
left join ds_mysql_prod_tagelement sta
on p.state = sta.id 
 
left join ds_mysql_prod_tagelement ci
on p.city = ci.id 

order by description desc)

select * from table1