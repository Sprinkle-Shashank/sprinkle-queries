with sub1 as (
select projectid, name, project_name,sched7,fy_timerange, project_type from profile_41363),
sub2 as (
  select projectid, name, project_name,fy_timerange,sched,project_type from sub1 s1, s1.sched7 sched) 
	select p.name as projectid, s2.name, project_name, t.name as sched7, fy_timerange, t1.name as choose from sub2 as s2
   left join ds_mysql_prod_tagelement t 
   on t.id=sched
   left join ds_mysql_prod_project p
   on p.id=projectid
   left join ds_mysql_prod_tagelement t1 on t1.id = s2.project_type
