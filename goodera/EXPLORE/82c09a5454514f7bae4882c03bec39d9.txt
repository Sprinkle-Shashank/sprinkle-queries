select pro.name as ProjectId, theme.name as theme, fy_timerange, 'Budget Allocated' as indicator, amount_allocated from profile_79341 p
  LEFT JOIN ds_mysql_prod_project AS pro
  ON p.projectid = pro.id
  LEFT JOIN ds_mysql_prod_project AS theme
  ON pro.parent = theme.id