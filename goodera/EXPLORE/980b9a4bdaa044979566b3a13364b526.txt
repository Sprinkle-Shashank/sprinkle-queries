with table1 as
(
  
  select p.projectid , country, fac_cl_vpid, vendor_ngo_id, primary_category from profile_52810 p
  
  ),
  
  table2 as
  (
select p.projectid , fac.name as factory_name, ven.name as vendor, cou.name as country from profile_64075 p

left join ds_mysql_prod_project fac
on  p.projectid = fac.id

left join ds_mysql_prod_project ven
on fac.parent = ven.id

left join ds_mysql_prod_project cou
on ven.parent = cou.id
	)
	
	select * from table1