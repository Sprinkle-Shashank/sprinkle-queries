SELECT pro.name as projectid,fy_timerange,
SUM(teachers_non_avasara) AS count

FROM profile_79096 S

LEFT JOIN ds_mysql_prod_project AS pro
ON S.projectid = pro.id

GROUP BY pro.name ,fy_timerange, teachers_non_avasara