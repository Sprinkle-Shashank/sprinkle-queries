with phase1 as (
	select proj.name as projectId, CAST(p.fy_timerange.start||'|'||p.fy_timerange.end as varchar) as fy_timerange, p.unit_name, 
  	p.nomember from profile_58866 p
  	left join ds_mysql_prod_project proj
  	on p.projectid = proj.id
)
select * from phase1