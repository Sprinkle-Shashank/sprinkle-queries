with table1 as
( select proj.name as projectname, fy_timerange, ent.name as still_employed,ent1.name as curr_income from profile_83716 as p1
 
 left join ds_mysql_prod_project proj
 on p1.projectid = proj.id
 
  
 left join ds_mysql_prod_tagelement ent
 on p1.still_employed = ent.id
 
  left join ds_mysql_prod_tagelement ent1
 on p1.curr_income = ent1.id
 
 )
 
 select * from table1