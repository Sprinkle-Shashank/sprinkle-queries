select _id, projectId.name as projectId, fy_timerange, photo, remarks, projs.name as project_name from profile_74731 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
left join ds_mysql_prod_tagelement as projs
on m.project_name = projs.id
order by fy_timerange desc