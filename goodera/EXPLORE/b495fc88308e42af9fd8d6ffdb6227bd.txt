WITH table1 AS (SELECT *  
				FROM profile_51331	
				),

table2 AS (SELECT t1.*, ngo_name, state_name FROM table1 t1, 
		   t1.ngo AS ngo_name,
		  t1.state AS state_name
		  ),
table3 AS (SELECT * FROM profile_51331 
		   WHERE _id NOT IN (SELECT _id FROM table2)

/*SELECT sdg_goal, project_pic, pro.name AS projects, ng.name AS ngo, fy.name AS fy_1, ps.name AS project_status, the.name AS theme, description, sdg.name AS sdg_goal_number, fy_timerange, year_of_inception, st.name AS state, cy.name AS city

FROM profile_51331 p

LEFT JOIN tagElement_4886 pro
ON p.projects = pro.id

LEFT JOIN tagElement_4886 the
ON p.theme = the.id

LEFT JOIN tagElement_4886 ps
ON p.project_status = ps.id

LEFT JOIN tagElement_4886 fy
ON p.fy_1 = fy.id

LEFT JOIN tagElement_4886 ng
ON p.ngo = ng.id

LEFT JOIN tagElement_4886 sdg
ON p.sdg_goal_number = sdg.id

LEFT JOIN tagElement_4886 st
ON p.state = st.id

LEFT JOIN tagElement_4886 cy
ON p.city = cy.id*/

SELECT * FROM table3