with table1 as
(
  select name from Profile_60398 as p
  
  ),
  
  table2 as
  (
	select name, count(*) as count_update from table1
	
	group by name
	
	order by name ASC
  )
	
   select name, count_update from table2