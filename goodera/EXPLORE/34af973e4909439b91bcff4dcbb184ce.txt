select p1.name as location, count(*), 

from profile_51990 p

left join ds_mysql_prod_project fac on p.projectid = p1.id
--left join ds_mysql_prod_project fac on fac.parent = fac2.id
group by location