with sub1 as(
  select a.*, projects_unwind from profile_76630 a, a.projects projects_unwind)
  
select a.timerange_timerange as fy_timerange,t1.name as projects,a.target from sub1
left join ds_mysql_prod_tagelement t1 on t1.id=a.projects_unwind