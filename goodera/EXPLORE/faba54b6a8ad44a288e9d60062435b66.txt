with t1 as
(
  select projectId, line_item, date, amount_utilized, p1.direct_indirect
  from profile_56356
  
  left join profile_56355 p1 on
 p.line_item = p1._id
  ),
  
  t2 as
  (
	select projectId, line_item, date, p1.direct_indirect, sum(amount_utilized) as Total_Amount_Utilized from t1
	
	group by projectId, date, direct_indirect, line_item
	
	)
	select * from t2
  