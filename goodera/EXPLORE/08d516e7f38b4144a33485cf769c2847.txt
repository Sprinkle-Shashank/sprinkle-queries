with phase1 AS (
	select proj.name as projectId, SUBSTRING(cast(p.joindate as varchar),1,10)||'T00:00:00.000Z' as joindate,decode(p.dropdate,null,current_date||'T00:00:00.000Z',p.dropdate) as dropdate, p.member from profile_58858 p
  	left join ds_mysql_prod_project proj
  	on p.projectid = proj.id
)
select * from phase1