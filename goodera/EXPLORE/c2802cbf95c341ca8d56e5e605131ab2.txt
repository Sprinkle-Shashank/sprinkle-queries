select pro.name as projectid,certificate_start_date,certification_end_date,topic,unit_name from profile_82817 p
left join ds_mysql_prod_project pro
on p.projectid = pro.id


/*select name,fy_timerange,keyword,datanum from (select name,fy_timerange,keyword,datanum,row_number () over(partition by keyword order by fy_timerange desc) as row from sus_80478

where keyword in ('carbon_neutral_units','green_building_units') )

where row = 1

group by name,fy_timerange,keyword,datanum*/