SELECT p1.spoc_name, p1.designation, p1.contact, p1.email_id, p1.name as hospital, sta.name as state,dist.name as district,address 
FROM profile_79291 p
left join ds_mysql_prod_tagelement dist
on p.state = dist.id
left join ds_mysql_prod_tagelement sta
on dist.parent = sta.id 
left join profile_79292 p1 on
p.hospital = p1._id
