select p.projectId, p.placed, concat(cast(p.fy_timerange.start as varchar), concat('|', cast(p.fy_timerange.end as varchar))) as fy_timerange1, p.batch_id,h.start, h.end, h.donor, h.donor_commitment from profile_145

left join profile_196 h on
p.batch_id=h._id
