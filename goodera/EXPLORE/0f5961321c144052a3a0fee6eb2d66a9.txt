
with table1 as
(
select count(p.projectid) , p._id , projid_rp , p.name , 'Completed' as progress_update from profile_54646 as p
  
  group by  p._id , projid_rp , p.name 
  )
  
 /* table2 as 
  (
	select count(p.projectid) , projectid ,  p._id , projid_rp , p.name ,  progress_update from table1 as p
	
	group by  projectid ,  p._id , projid_rp , p.name 
	)*/
  
  select p.* , from table1 as p