with table2 as 
(
  select * from profile_76755 as p
 ),
  
 table1 as 
( select p.projectid , p._id , indepth_workers, indepth_managers from profile_76894 as p
 )
 
 select * from table2
 
 /*
 
 
   select p.projectid , fac.name as factory , ven.name as vendor , cou.name as country , case when extract(month from year_timerange)=1 then extract(year from year_timerange)-1 
else extract(year from year_timerange) end as year from profile_76755 as p
  
left join ds_mysql_prod_project fac 
on p.projectId = fac.id

 left join ds_mysql_prod_project ven 
on fac.parent = ven.id

 left join ds_mysql_prod_project cou 
on ven.parent = cou.id 

 
-- where year != 2021 and country <> 'Test Country' and p.projectid ilike '%%'
*
/