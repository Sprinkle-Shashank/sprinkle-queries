with table2 as 
(
  select year_timerange , p.projectid , p._id , p.name from profile_76755 as p
 ),
  
 table1 as 
( select p.projectid , p._id , indepth_workers, indepth_managers , fac.name as factory , ven.name as vendor , cou.name as country from profile_76894 as p

left join ds_mysql_prod_project fac 
on p.projectId = fac.id

 left join ds_mysql_prod_project ven 
on fac.parent = ven.id

 left join ds_mysql_prod_project cou 
on ven.parent = cou.id)
 
 select * from table1
 