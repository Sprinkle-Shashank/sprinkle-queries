with temp1 AS
(select m.projectId, m.project_name, m.vertical, m.focus, cast(m.start_date as date) as start_date, m.project_code, m.entity, r.completed, r.fy_timerange, r.progress
  
from profile_62881 as m

left join profile_55471 AS r
 on m.project_code = r.project_code)

select m.projectId, m.project_name, m.vertical, m.focus, cast(m.start_date as date) as start_date, m.project_code, m.entity, m.completed, m.fy_timerange, m.progress from temp1 as m
 
