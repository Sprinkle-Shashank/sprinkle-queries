with table1 as
( select p.projectid , p._id , fac.name as factory , case when nvl2(SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), ven.name)='' then ven.name else nvl2(SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), ven.name) end as vendor , cou.name as country , p1.factory_id, p2.vpid as vendor_id , sta.name as status_update , case when status_update is null then 'null Yes' when status_update=' ' then 'space Yes' end as status_upate , trim(' ' from status_upate) as trim_update , case when trim_update is null then 'null Yes' when trim_update=' ' then 'space Yes' end as status_upate_new from profile_16619 as p 

left join ds_mysql_prod_project fac
on  p.projectid = fac.id

left join ds_mysql_prod_project ven
on fac.parent = ven.id

left join ds_mysql_prod_project cou
on ven.parent = cou.id
 
left join ds_mysql_prod_tagelement sta
on p.status = sta.id

left join profile_4722 p1 
on p.projectid = p1.projectid 
 
left join profile_4718 p2 
on p.projectid = p2.projectid 
 
where cou.id != 13761 
)


select * from table1 as p