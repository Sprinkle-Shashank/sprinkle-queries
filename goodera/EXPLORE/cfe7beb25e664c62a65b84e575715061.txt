with sub1 as (select a.*, unwind_file from profile_75323 a, a.file as unwind_file)
select _id, projectid, fy_timerange, cast(unwind_file as varchar) as unwind_file, remarks, doc_type, projects, link, theme
from sub1
where unwind_file is null

