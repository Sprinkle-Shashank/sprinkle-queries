select concat(p.f_reg_date,concat('|',p.f_reg_date)) as fy_timerange, p.projectid, r.name as project, p.f_category, cast('COVID 19' as text) as disease, p.covid_tested, t.name as covid from profile_59462 p 
left join ds_mysql_prod_tagelement t on p.covid_tested = t.id 
left join ds_mysql_prod_project r on p.projectid = r.id where covid_tested is not null