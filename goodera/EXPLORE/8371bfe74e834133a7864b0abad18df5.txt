select p.name as projectId, a.fy_timerange,p1.name as beneficiary,count(*)
from profile_81392 a
left join ds_mysql_prod_project p on p.id=projectid

left join profile_81375 p1 on p1._id= a.beneficiary


group by p.name,a.fy_timerange,p1.name
