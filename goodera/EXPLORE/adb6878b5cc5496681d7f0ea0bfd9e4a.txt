select p.projectId, p.batch_id, p.placed, p.center_id, p.fy_timerange,h.start, h.end, h.donor, h.donor_commitment,k.partner_name,t2.name as project from profile_145 p

left join profile_196 h on
p.batch_id=h._id

left join profile_141 k on
p.center_id=k._id

LEFT JOIN ds_mysql_prod_project t on p.projectId=t.id
LEFT JOIN ds_mysql_prod_project t1 on t.parent=t1.id      
LEFT JOIN ds_mysql_prod_project t2 on t1.parent=t2.id
