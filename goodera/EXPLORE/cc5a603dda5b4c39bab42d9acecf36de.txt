with temp as (
  select 'Uniforms' as col
  union all
  select 'Kits' as col
  union all
  select 'laptops' as col
)


select fy_timerange, projectid, (SUM(uniforms)+ SUM(kits)+ SUM(laptops)) as "Total Items"
from profile_78771
group by fy_timerange, projectid



