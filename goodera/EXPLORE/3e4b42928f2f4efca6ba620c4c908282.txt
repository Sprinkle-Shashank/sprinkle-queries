with temp1 as
(select _id, projectId, state, phc, pic_1_before, cap_1_before, fy_timerange, CAST(photo AS varchar)
 from profile_76555 as m, m.pic_1_before photo),
 
 temp2 as
 (select _id, projectId, state.name as state, phc, pic_1_before, cap_1_before, fy_timerange, photo
  from temp1 as m
  
  left join ds_mysql_prod_tagelement as state
  on m.state = state.id)
  
  select * from temp2
 
 
 