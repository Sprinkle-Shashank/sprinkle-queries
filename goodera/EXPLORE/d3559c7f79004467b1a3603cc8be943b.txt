with table1 as(select parent.name as projectId, name_ben as title, story, pic, fy_timerange, desgination from profile_55310 p

left join ds_mysql_prod_project as pro
             on p.projectid = pro.id
		left join ds_mysql_prod_project as parent
             on pro.parent = parent.id	  ),
table2 as( select projectId, fy_timerange, desc_use_case, photo, file, title_use_case, who_used, select_device from profile_75391)

select * from table1