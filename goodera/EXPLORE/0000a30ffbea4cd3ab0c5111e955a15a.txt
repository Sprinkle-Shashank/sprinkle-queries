with temp1 AS
(select m._id AS old_id, m.projectId, m.indicator, m.sdgs, m.schedule_vii, m.proj, m.project_name, m.ben_type, m.baseline, m.target, m.name, r._id, r.fy_timerange, r.project_name, r.vertical, r.focus, r.state, r.direct_or_partner, r.project_code, r.entity, partner.name AS partner_1, q.fy_timerange, q.progress


from profile_61044 AS m

left join profile_62881 AS r 
 on m.proj = r._id
 
left join ds_mysql_prod_tagelement as partner
  on r.direct_or_partner = partner.id
  
left join profile_61046 AS q 
 on m.old_id = q.indicator
 
where r.direct_or_partner = 1132991),

temp2 as
(select p.*, q1 from temp1 as p, p.vertical as q1)

select m.old_id, m.projectId, m.indicator, m.sdgs, m.schedule_vii, m.proj, m.project_name, m.ben_type, m.baseline, m.target, m.name, m._id, m.project_name, m.vertical, m.focus, m.state, m.direct_or_partner, m.project_code, m.entity, m.partner_1, m.fy_timerange, m.progress from temp2 AS p

