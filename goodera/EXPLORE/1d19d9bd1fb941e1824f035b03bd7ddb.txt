with table1 as
(
  select  projectId, mobilization_activity, time_timerange, cast(fy as VARCHAR) as name_up, state, mob_num
  
  from Profile_60366 as p
  ),
  
  table2 as
  (
	select sum(mob_num) as mob_num_update, projectId, mobilization_activity, name_up, state from table1
	
	group by projectId, mobilization_activity, fy, state
	)
	select projectId, mobilization_activity, name_up, state, mob_num_update from table2
	