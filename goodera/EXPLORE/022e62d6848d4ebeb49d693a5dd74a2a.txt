
with t1 as 
(select p1._id, p1.fy_timerange, p1.line_item, p1.category, p1.budgeted_amount, p1.projectId, p2.date :: varchar, p2.amount_utilized :: varchar
from profile_74913 p1
left join profile_74914 p2
on p1._id = p2.line_item)

select _id, fy_timerange, line_item, category, budgeted_amount, projectId,
first_value(t1.date) over (partition by t1.date order by t1.date rows between unbounded preceding and unbounded following) as date,
first_value(amount_utilized) over (partition by t1.amount_utilized order by t1.date rows between unbounded preceding and unbounded following ) as amount_utilized
from t1








