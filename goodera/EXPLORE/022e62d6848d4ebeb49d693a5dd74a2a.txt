
select p1._id, p1.fy_timerange, p1.line_item, p1.category, p1.budgeted_amount, p1.projectId,
first_value(p2.date) over (partition by p2.date order by p2.date rows between unbounded preceding and unbounded following) as date,
first_value(p2.amount_utilized) over (partition by p2.amount_utilized order by p2.date rows between unbounded preceding and unbounded following ) as amount_utilized
from profile_74913 p1
left join profile_74914 p2
on p1._id = p2.line_item








