with temp1 as
(select m._id, m.indicator /*as indicator_ref*/, m.project_code, m.fy_timerange, m.progress, r.description, r.indicator, r.target, q.name, q.project_name, q.vertical, q.focus, q.state, q.direct_or_partner, q.district, q.entity

 from profile_61046 as m
 
 left join profile_61044 AS r
 on m.indicator = r._id

 left join profile_62881 AS q
 on m.project_code = q.project_code)
 
--temp2 as
 select * from temp1
 
