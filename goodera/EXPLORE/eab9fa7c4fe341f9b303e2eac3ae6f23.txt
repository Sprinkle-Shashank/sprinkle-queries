select a.projectid,a._id,a.name,case when b.name is not null then b.name else 'blank' end as doc_type, a.doc_name,a.doc from profile_54315 a
left join ds_mysql_prod_tagelement b
    on a.doc_type = b.id
	
	