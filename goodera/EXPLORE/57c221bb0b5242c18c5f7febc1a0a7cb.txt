SELECT id, projectId, state, phc, pic_1_before, cap_1_before, fy_timerange
FROM (
  SELECT id, projectId, state, phc, pic_1_before, cap_1_before, fy_timerange
  FROM profile_76555
) t
CROSS JOIN UNNEST (pic_1_before) WITH ORDINALITY AS pic(value, idx)
