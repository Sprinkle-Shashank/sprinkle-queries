SELECT id, projectId, state, phc, pic_1_before, cap_1_before, fy_timerange
FROM (SELECT id, projectId, state, phc, UNNEST(pic_1_before) AS pic_1_before, cap_1_before, fy_timerange FROM profile_76555) AS unpivoted_data
