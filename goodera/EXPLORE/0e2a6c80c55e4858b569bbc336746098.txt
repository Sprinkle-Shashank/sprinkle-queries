select count(a.uid)as val,b.name as batch_year, a.start_date as batch_start_date, c.name as course_name,a.fy_timerange,
a.a.end_date  as batch_end_date,count(*)

from  profile_79247 a
 left join ds_mysql_prod_tagelement b on a.uid=b.id
 left join ds_mysql_prod_tagelement c on a.course_name=c.id
 
 
where a.end_date is not null 
group by b.name, a.start_date  ,c.name,a.fy_timerange,
a.end_date