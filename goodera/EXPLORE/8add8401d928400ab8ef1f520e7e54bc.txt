select  projectId, overall_workers, roi, wep_data_collection, start_date, improvements, reason, features, others, survey_yn, shared_results, notes, num_workers, num_positive, turnover, num_workers_total, num_registered, num_active, overall_valuable,ven.name as vendor,cou.name as country,fac.name as factory from profile_60782

left join ds_mysql_prod_project fac
on profile_60782.projectid = fac.id
left join ds_mysql_prod_project ven
on fac.parent = ven.id
left join ds_mysql_project cou
on ven.parent = cou.id