with base1 as (Select cast((split_part(split_part(p.name,'|',1),'.',1)) as varchar) as ProjectCode, cast(split_part(p.name,'|',3) as varchar) as census_code, f.name as fy_timerange,CONCAT(ProjectCode,fy_timerange) as CODE
from profile_54963 sq
left join ds_mysql_prod_project p
on p.id=sq.projectid)

Select* from base1
