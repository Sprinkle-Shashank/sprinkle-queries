select f.name as fy_timerange,split_part(prod.name,'|',3),b.name,case when b.name is not null then 1 else 0 end as Count from profile_32571 p
left join ds_mysql_prod_project prod on
prod.id=p.projectid
left join ds_mysql_prod_tagelement b on
p.bene=b.id
left join ds_mysql_prod_tagelement f on
p.fy=f.id
