with t1 as
(
  select _id, projectId, proj.name as proj_up, p.name, doc_type, upload, other_remarks, remarks, fy_timerange, projects,
  case when upload<>'' 
  then 'Download' else '-'
  end as Download
  from profile_56248 as p
  
  left join ds_mysql_prod_project proj
  on p.projectid = proj.id
  
  order by fy_timerange DESC
  )
  select * from t1
  
  