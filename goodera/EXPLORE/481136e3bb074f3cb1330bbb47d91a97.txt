with t1 as
(
  select id, projectId, name, doc_type, upload, other_remarks, remarks, fy_timerange, projects,
  case when upload<>'' 
  then 'Download' else '-'
  end as Download
  from profile_56248 as p
  
  
  )
  select * from t1
  sort by fy_timerange DESC
  