with table1 as (
select count(distinct project_name) as total_long_projects from profile_79500

), table2 as (
select count(distinct project_name) as total_short_projects from profile_80250

)select table1.total_long_projects + table2.total_short_projects