


with sub as 
(
select 
 projectid, p.name, date, other, local_foreign, no_local_foreign, p.location, notes, survey_yn, excel_upload, tr_created, keyword_1, num_managers, hours, num_middle, num_other, participants,tri.name as training_type, trainer_organization, trainer, ven.name as vendor, cou.name as country, nvl2(SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), SUBSTRING(ven.name, 1, POSITION(' (' in ven.name)), ven.name) as vendor_name  from profile_61572 as p
 
 left join ds_mysql_prod_project fac 
on p.projectId = fac.id

 left join ds_mysql_prod_project ven 
on fac.parent = ven.id

 left join ds_mysql_prod_project cou 
on ven.parent = cou.id

left join ds_mysql_prod_tagelement tri 
on p.training_type = tri.id
), sub2 as
(
select s1.*,c from sub as s1, s1.trainer_organization c
  
 where country <> 'Test Country'
)


select s2.*,triorg.name as trainer_organization from sub2 as s2

left join ds_mysql_prod_tagelement triorg 
on s2.c = triorg.id





/*
group by 
projectId, name, date, other, local_foreign, no_local_foreign, location, notes, survey_yn, excel_upload, tr_created, keyword_1, num_managers, hours, num_middle, num_other, participants, training_type, country, vendor, year, trainer_organization, trainer, vendor_name
*/
/*

select 
s2.projectId, s2.name, date, other, local_foreign, no_local_foreign, s2.location, notes, survey_yn, excel_upload, tr_created, keyword_1, num_managers, hours, num_middle, num_other, participants, training_type, triorg.name as trainer_organization, tra.name as trainer, country, vendor,  

 extract(month from date)
as m1 , extract(year from date) as simple_year,
case when m1=1 then simple_year-1
else simple_year
end as year
, vendor_name 
	  
from sub2 as s2

left join ds_mysql_prod_tagelement triorg 
on s2.c = triorg.id

left join ds_mysql_prod_tagelement tra 
on s2.d = tra.id

where country <> 'Test Country'

group by 
projectId, s2.name, date, other, local_foreign, no_local_foreign, location, notes, survey_yn, excel_upload, tr_created, keyword_1, num_managers, hours, num_middle, num_other, participants, training_type, country, vendor, year, triorg.name, tra.name, vendor_name
*/
/*
Group By
Fields: projectId, name, date, other, local_foreign, no_local_foreign, location, notes, survey_yn, excel_upload, tr_created, keyword_1, num_managers, hours, num_middle, num_other, participants, training_type, trainer_organization, trainer, country, vendor_name, vendor, year
*/