SELECT  pro.name as projectid,fy_timerange, s.name, sum(total_students) as count 

FROM profile_79044 S

LEFT JOIN ds_mysql_prod_project AS pro
ON S.projectid = pro.id

group by pro.name ,fy_timerange, s.name