select tag1.name as projectid, _id, p1.name, date_of_admission, age, background, fg_learner, reason_admission, school_name, class, fy_timerange, shelter_home, shelter_home_text, school_type, medium, child_status
from profile_52387 p1
left join ds_mysql_prod_project tag1 
on p1.projectid = tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.age = tag2.id
where p1.name = 'Amulya R'



