with temp1 as
( select m.name, m.fy_timerange, SUM(r.amount_disbursed) as disbursed, SUM(m.amount_utilized) as Utilized from profile_80774 as m

left join profile_81476 AS r
 on m.name = r.name


)
select * from temp1