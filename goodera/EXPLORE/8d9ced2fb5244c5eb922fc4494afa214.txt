select _id, projectId, p_start, p_end, a_start, a_end, delay, milestone,CURRENT_TIMESTAMP as cur_date,
CASE
    WHEN a_end is null THEN CURRENT_TIMESTAMP
   
    ELSE a_end
END AS new_endDate, p_start as fy_timerange, proj.name as proj_name
from profile_76544 p

left join ds_mysql_prod_tagelement proj
on P.projectId= proj.id