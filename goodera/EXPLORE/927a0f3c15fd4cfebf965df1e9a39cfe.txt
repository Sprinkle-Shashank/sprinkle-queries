with table1 as 
(
  select name_country,start_quarter, start_year,  num_females, lg_status, select_modules

FROM profile_4227 


  )
  , table2 as 
  (
	select tab1.*,c from table1 as tab1, tab1.select_modules c
)
select name_country,start_quarter, start_year,  num_females, lg_status, profile_54646.name_vendor, profile_54646.name_vendor_filter,select_modules, triorg.name as select_modules from table2 as tab2

LEFT JOIN profile_54646 ON
profile_4227.projectId = profile_54646.projectId
  
left join ds_mysql_prod_tagelement triorg 
on tab2.c = triorg.id
  
where (lg_status = 848007 or lg_status is null)
