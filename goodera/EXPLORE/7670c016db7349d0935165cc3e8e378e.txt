with s1 as (
  Select p1.name projectid, sq.name,CAST(listagg(DISTINCT sq.photo) as VARCHAR) photo
FROM profile_79653 sq
left join ds_mysql_prod_project p1
ON p1.id=sq.projectid
  GROUP BY sq.name,p1.name
)
Select name, CAST(pic AS VARCHAR), projectid
from s1 a,a.photo pic
