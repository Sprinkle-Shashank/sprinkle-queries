with temp1 as
(select m.projectId, m.indicator, m.proj, m.target
from profile_61044 as m)

/*left join ds_mysql_prod_tagelement as vert
on m.vertical_new = vert.id*/

 left join profile_55194 AS r 
 on m.proj = r.project_code_new
 
 left join ds_mysql_prod_tagelement as year
 on r.year = year.id
 
 left join ds_mysql_prod_tagelement as project_code_new
 on r.project_code_new = project_code_new.id),

select m.projectId, m.indicator, m.proj, m.target, m.project_code_new from temp1  
