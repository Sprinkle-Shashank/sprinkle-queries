select timestamp 'epoch' + cast(created as decimal(20,5)) * interval '1 second' as create_date from profile_54318
/*select p1.name as projectId, p.file, p.name, t1.name as file_type, p.project_name, p.timerange_timerange as fy_timerange, p.desc, p.created, 'Download' as title
	from profile_54318 p
	left join ds_mysql_prod_project p1 on p1.id = p.projectid
	left join ds_mysql_prod_tagelement t1 on t1.id = p.file_type*/
	


	