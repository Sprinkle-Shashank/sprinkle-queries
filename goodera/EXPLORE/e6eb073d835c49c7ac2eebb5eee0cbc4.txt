with sub1 as(select p1.name as projectid,p1.fy_timerange as Timerange,
Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,
			 Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,
			 Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,
			 Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,Sum(CASE 
             WHEN keyword = 'personal_vehicle' THEN datanumBase
             ELSE 0 
           END)  as personal_vehicle,
			 
from sus_64637
where p1.keyword in('personal_vehicle','motorbike_mileage','bus_mileage','train_mileage','metro_mileage','walk_mileage','bicycle_mileage','other_mileage','tram_mileage','scooter_mileage') and p1.datanumbase is not null)

select name,Timerange,sum(commute_type) as commute
group by 	