with temp1 as
select m.projectId, m.date, m.heading, m.brief, m.photo, m.url,
--//concat(concat(mid(date,9,2) ,concat (-'(',mid(date,6,2,')')), concat (-'(',mid(date,6,2,')')))
to_char(m.date,'DD-MM-YYYY') as new_date, cast(new_date as Date) as fy_timerange
from profile_67584 as m
as mid_date
order by fy_timerange desc
	   
	   --//concat(mid($date,9,2),"-",mid($date,6,2),"-",mid($date,1,4))