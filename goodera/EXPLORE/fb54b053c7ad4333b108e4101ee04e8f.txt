select * from
( select 
fy_timerange,
demo_sessions,
cra_trainings,
per_farmers,
sus_farmers,
multi_layered_farm,
farmer_groups

from profile_83221)

Unpivot(
val for dim in(
demo_sessions,
cra_trainings,
per_farmers,
sus_farmers,
multi_layered_farm,
farmer_groups))
