select pro.name as projectId, fy_timerange, setoff_amount, setoff_amount_preceeding  , INITCAP(ent.name) as entity from profile_80648  

left join ds_mysql_prod_project as pro
  on projectId = pro.id	
  
left join ds_mysql_prod_tagelement ent 
on p.entity = ent.id