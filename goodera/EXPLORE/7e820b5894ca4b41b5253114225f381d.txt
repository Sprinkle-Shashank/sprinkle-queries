select p._id, p.projectid, p.name, p.donor, p.start, p.fy_timerange, p.amount, p.end,h.utilized, k.amnt,y.name as Project,concat(concat(substring(p.start,1,10),'-'),substring(p.end,1,10)) as Period from profile_14715 p

left join profile_56059 h on
p._id=h.donor_com

left join profile_25910 k on
p._id=k.donor_com

--LEFT JOIN ds_mysql_prod_project t on p.projectId=t.id
--LEFT JOIN ds_mysql_prod_project t1 on t.parent=t1.id      
--LEFT JOIN ds_mysql_prod_project t2 on t1.parent=t2.id

LEFT JOIN ds_mysql_prod_project y on p.projectid=y.id