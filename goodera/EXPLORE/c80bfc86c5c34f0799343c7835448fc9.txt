
select year_timerange, subsection_title, content, report_section,d.name as gri_sec, d1.name as djsi_sec, /*LISTAGG(DISTINCT cast (gri_sec as int),',' ) as gri_section ,
 
LISTAGG(DISTINCT cast (djsi_sec as int),',' ) as djsi_section */
sort from  profile_62982 
left join ds_mysql_prod_tagelement d
on d.id=gri_sec
left join ds_mysql_prod_tagelement d1
on d1.id=djsi_sec
/*group by year_timerange, subsection_title, content, report_section, sort
order by sort*/

