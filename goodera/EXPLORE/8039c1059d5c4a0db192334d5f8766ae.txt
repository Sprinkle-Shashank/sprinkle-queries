with table1 as (SELECT json_serialize(fy_timerange) as time from profile_79503)
select Substring(time,10,5) from table1

--case when get_array_length(sq.source_div) = 0 then array(0)   else COALESCE(sq.source_div,array(0)) end new_source
--STRPOS(string, substring )
--select Substring(time, STRPOS(time, 'start'), (STRPOS(time, 'end')-STRPOS(time, 'start'))) from table1