SELECT source_div, timeperiod_timerange, region_parent, factory_id, higg_requirement, extract(month from cast(timeperiod_timerange.start as date)) as month, case when month=1 then concat(SUBSTRING(timeperiod_timerange.start-1000),1,4)) else concat(SUBSTRING(timeperiod_timerange.start),1,4)) end as fy
from profile_60928 sq
