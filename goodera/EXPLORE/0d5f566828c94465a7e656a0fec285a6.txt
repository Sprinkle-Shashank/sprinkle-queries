select p.fy_timerange.start as fy_timerange, project.name as projectid, 'Allied' as f_category, cast('near_vision' as text) as vision,  (decode(nv_allied,null,0,nv_allied)  + decode(nv_partner,null,0,nv_partner)) as final_sum from profile_63886 p left join ds_mysql_prod_project as project
on project.id = p.projectid where final_sum is not null