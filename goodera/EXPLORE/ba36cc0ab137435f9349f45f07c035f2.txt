select proj.name as projectId, proj.name as center,batchid, course, fy_timerange,count(*) from profile_53367 p

left join ds_mysql_prod_project proj
  on p.projectId= proj.id

left join profile_46326 proj1
  on p.projectId= proj1.id

Group By projectId, batchid, course, fy_timerange,t.name,proj.name,proj1.name