select p.projectId, proj.name as center, check_new.name as check_update , batchid, course, fy_timerange,count(*) from profile_53367 p

left join ds_mysql_prod_project proj
  on p.projectId= proj.id
  
left join ds_mysql_prod_project check_new
 on proj.parent= check_new.id

Group By p.projectId, batchid, course, fy_timerange,proj.name , check.name