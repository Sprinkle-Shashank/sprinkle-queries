
select
with table1 as(select id, projectId, state, phc, pic_1_before, cap_1_before, fy_timerange from profile_76555),

table2 as (select t1.*, pic_1_before from table1 t1 , t1.pic_1_before pic_1_before)

select id,  state, phc,  cap_1_before, fy_timerange, pic_1_before , pro.name as project_name  from table2 p

LEFT JOIN ds_mysql_prod_project as pro on
p.projectid = pro.id

