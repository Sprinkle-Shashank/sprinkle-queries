select _id, projectId.name as projectId, date as fy_timerange, media, post_link, remarks, photo, MID(p.date, 1, 10) AS date_updated
from profile_63882 as p

left join ds_mysql_prod_project as projectId
  on p.projectId = projectId.id 
 