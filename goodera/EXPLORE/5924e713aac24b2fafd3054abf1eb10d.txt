select  projectId.name as projectId, projects, ngo, fy_timerange, Sum(m.budget_allocation) as budget_allocation , theme from profile_51028 as m
left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
group by projectId.name, projects, ngo, fy_timerange, theme
