select _id, batch_id, center_name, start_date, end_date, count(batch_id) batches, count(center_name) centers, sum(trainees) no_trainees, sum(trainers) no_trainers, sum(training_hours) no_training_hours, sum(training_days) no_training_days
from profile_76978 p
group by 1,2,3,4,5;