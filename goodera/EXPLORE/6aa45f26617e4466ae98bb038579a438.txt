with s as (Select 
		   attachments,dataselect,keyword
from sus_79338
where keyword in ('system_internally_audited,'occ_health_and_safety_management','system_audited_external_party')
)
Select  attach,dataselect,keyword
from s as p, p.attachments attach
/*Select
  (CASE 
             WHEN keyword = 'occ_health_and_safety_management' THEN datatext
             ELSE null 
           END) AS occ_health_and_safety_management,
  (CASE 
             WHEN keyword = 'system_internally_audited' THEN datanumBase
             ELSE 0 
           END) AS system_internally_audited,
  (CASE 
             WHEN keyword = 'system_audited_external_party' THEN datanumBase
             ELSE 0 
           END) AS system_audited_external_party
from sus_79338
where keyword in ('occ_health_and_safety_management','system_internally_audited','system_audited_external_party')*/