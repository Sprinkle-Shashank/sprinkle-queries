with t1 as
(SELECT * 
FROM
(Select   projectId, fy_timerange, setoff_amount, setoff_amount_preceeding from profile_77139 p 


)
UNPIVOT (
    val FOR dim IN (COALESCE(setoff_amount_preceeding,0),COALESCE(setoff_amount,0)))

)

select  projectId, fy_timerange,sum(val) as val,dim from t1 as p

  Group By projectId, fy_timerange, Dim