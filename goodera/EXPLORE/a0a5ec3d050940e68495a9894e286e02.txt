with temp1 as
( select projectId.name as projectId, fy_timerange, last_yr_status, SUM(m.csr_spend) as Csr_spend ,SUM(m.last_yr_spend) as Last_year_spend from profile_44750 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
 
group by projectId.name, fy_timerange, last_yr_status
)
select * from temp1
 
