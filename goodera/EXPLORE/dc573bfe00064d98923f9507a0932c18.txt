SELECT institution, 
COUNT (name) AS count,
CONCAT(CONCAT(CONCAT(SPLIT_PART(start_date, ' ', 1),'T'),SPLIT_PART(start_date, ' ', 2)),'.000Z') AS fy_timerange,

CASE
	WHEN projectId IS NOT NULL
	THEN 100
	
	ELSE NULL
END AS targets

FROM profile_77914

GROUP BY institution, name, start_date, targets