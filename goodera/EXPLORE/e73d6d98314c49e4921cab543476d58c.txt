SELECT projectId, placement_sector, timerange_timerange,count(*) AS Number_of_beneficiaries
FROM profile_3864
WHERE placement_sector IS NOT NULL
GROUP BY placement_sector, timerange_timerange, projectId