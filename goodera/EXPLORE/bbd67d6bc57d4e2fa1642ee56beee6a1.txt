select tag1.name as projectId, p1.fy_timerange, p1.session_date, tag2.name as p1.select_course, tag3.name as year_join_scholarship, p1.venue, p1.select_topic, p1.select_name, p1.actual_hours, p1.actual_coordinator
from profile_64052 p1
left join ds_mysql_prod_project tag1
on p1.projectId = tag1.id
left join ds_mysql_prod_tagelement tag2
on p1.select_course = tag2.id
left join ds_mysql_prod_tagelement tag3
on p1.year_join_scholarship = tag3.id

