select _id, proj.name as  projectId, p.name, village as location, dof, cast(p.fy_timerange.start as varchar)||'|'||cast(p.fy_timerange.end as varchar) as fy_timerange, total_households, ews_households, bpl_households, (decode(male,null,0,male)+decode(female_total,null,0,female_total)) as Total_Beneficiary, female_ews, female_bpl, p.lat, p.long from profile_58845 p
left join ds_mysql_prod_project proj on p.projectId=proj.id