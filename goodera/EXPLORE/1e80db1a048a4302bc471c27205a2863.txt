select projectId.name as profileId, fy_timerange,city  from profile_64881 as m

left join ds_mysql_prod_project as projectId
on m.projectId = projectId.id
/*left join ds_mysql_prod_tagelement as city1
on m.city = city1.id
where city1 IS NOT NULL*/

group by projectId.name , city, fy_timerange
