with table2 as 
  (select p.projectid , fac.name as factory, ven.name as vendor, cou.name as country from profile_61483 as p

left join ds_mysql_prod_project fac
on p.projectid = fac.id

left join ds_mysql_prod_project ven
on fac.parent = ven.id

left join ds_mysql_prod_project cou
on ven.parent = cou.id
   
where t21.Active_Filter='Active' and p.projectid like '%%'
)

select * from table2