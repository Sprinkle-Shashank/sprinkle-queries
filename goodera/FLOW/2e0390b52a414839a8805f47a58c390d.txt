drop table if exists taskresp_27396;
 create table taskresp_27396 as
 SELECT profileinstanceid, status, data, approvallog,
  CAST(submissionlog.email as VARCHAR),
  CAST(submissionlog.timestamp as datetime)

 /*CAST(data.submissionlog as VARCHAR),
 CAST(GET_ARRAY_LENGTH(data.approvallog) as INT)*/
from ds_mongo_prod_taskresponse where profileId='27396' and active = 't'