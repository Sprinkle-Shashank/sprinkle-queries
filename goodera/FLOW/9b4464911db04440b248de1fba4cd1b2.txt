drop table if exists profile_79704;
 create table profile_79704 as
 SELECT projectid, _id,
 CAST(data.name as VARCHAR),
 CAST(data.fy_timerange as SUPER),
 CAST(data.households as INT),
 CAST(data.committees as INT)
from ds_mongo_profileinstance where profileId='79704' and active = 't' and securitycontextid is not null