drop table if exists profile_81235;
 create table profile_81235 as
 SELECT projectid, _id,
 CAST(data.name as VARCHAR),
 CAST(data.fy_timerange as SUPER),
 CAST(data.caption as VARCHAR),
 CAST(data.photo as SUPER)
from ds_mongo_profileinstance where profileId='81235' and active = 't' and securitycontextid is not null