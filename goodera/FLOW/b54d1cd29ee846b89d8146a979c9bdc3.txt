drop table if exists profile_60336;
 create table profile_60336 as
 SELECT projectid, _id,
 CAST(data.name as VARCHAR),
 CAST(data.fy_timerange as SUPER),
 CAST(data.disbursed as INT),
 CAST(data.allocated as INT),
 CAST(data.receipt_80g as VARCHAR),
 CAST(data.receipt_ackg as VARCHAR)
from ds_mongo_profileinstance where profileId='60336' and active = 't' and securitycontextid is not null