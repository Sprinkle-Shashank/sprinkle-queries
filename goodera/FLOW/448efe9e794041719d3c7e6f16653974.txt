drop table if exists profile_65005;
 create table profile_65005 as
 SELECT projectid, _id,
 CAST(data.name as VARCHAR),
 CAST(data.fy_timerange as SUPER),
 CAST(data.caption as VARCHAR),
 CAST(data.photo as SUPER)
from ds_mongo_profileinstance where profileId='65005' and active = 't' and securitycontextid is not null